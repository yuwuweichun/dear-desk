# Dear Desk 首版产品规格

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已批准 |
| 产品阶段 | 首版验证 |
| 创建日期 | 2026-08-06 |
| 当前事实 | 已实现本子聚焦开合、独立 Sticker Forge 图片工作台与桌面/日记双目标、双页仿真翻页；`DD-20260809-004` 已实现左页留白、右页单日期及今天/历史统一阅读与书写流程；`DD-20260810-002` 已按 `animal-island-ui` 视觉语言重写通用控件与贴纸工作台，并删除无持续职责的常驻顶栏，当前待用户验收 |
| 来源 | 用户关于“痕迹工作台”的产品设想与首版建议 |

> 本文定义已批准的产品范围，不代表当前已经存在对应源码能力。具体技术方案与实施范围仍需通过独立任务审批。

## 1. 产品定位

Dear Desk 不是一个以 3D 画面为卖点的日记工具，而是一个会随着使用逐渐留下痕迹的私人工作台。

它的首版目标是验证用户是否愿意反复回到同一张桌子，并对自己积累的空间状态产生记忆和珍惜。Three.js 服务于空间记忆、物体手感和时间沉积，不单独作为展示效果存在。

## 2. 目标用户行为

用户可以在三分钟内完成一次轻量记录：

```text
坐下 -> 打开本子留下文字，或独立制作一张贴纸 -> 放在桌面或日记 -> 离开
```

用户不必每天写长文。一次有效记录可以只是一句话、一张照片或一个当天关键词。

## 3. 首版要验证的问题

1. 用户是否愿意连续七天回到工作台。
2. 用户是否开始记住某件内容被放在什么位置。
3. 用户是否因为已有痕迹而不愿意重置桌面。

第三个问题是首版最重要的产品信号。它代表桌面开始形成普通日记工具缺少的情感壁垒。

## 4. 建议首版范围

### 4.1 一张可持续保存的桌面

- 桌面具有稳定视角和有限的可放置区域。
- 用户摆放的内容在重新打开页面后仍然存在。
- 当前默认视觉采用“明亮轻拟物 Animal Desk”材质版本 `V2.0`：场景使用冷灰背景，程序化中深胡桃木桌为无抽屉岛台，包含一块厚圆角台面、左右板式支撑和中央横梁，不包含柜体、抽屉或拉手；蓝灰织物桌垫由较深软 bumper、较浅内嵌工作场、暖白分段织线与珊瑚角部 tab 组成，贴纸交互范围保持稳定。背景、胡桃木承载面和蓝灰工作面支持现有墨绿色本子，使本子保持最深视觉焦点，同时避免已否决 V1 的粉米木桌与婴儿薄荷桌垫组合。3D 本子采用长边:短边 `5:3` 的竖向手帐比例和硬壳平背结构，前后封面、窄直背与内凹书槽使用本子专属的墨绿色哑光牛皮纸 PBR 材质，闭合时由两片薄硬壳封面夹住一个完整厚页芯，三条暖白纸口自然内缩，不再使用圆柱/胶囊书脊、分离的左右闭合页芯或软垫式多层封面，也不包含 3D 书签、封面角标、装饰圆钉或粉色书脊装饰。木、布、纸与牛皮纸使用确定性的独立 PBR 通道；木与布的 albedo 只提供中性明暗细节，表面基色由角色化 palette token 统一拥有，本子与纸张的颜色、纹理和材质参数保持不变。打开时前封面先绕封面左外缘机械轴展开，随后 5 张独立视觉页片错峰复用，表现约 9 次从右页根向左侧快速逐页翻动；完整页芯保持静止，页片清场并短暂停顿后，由 DOM 双页使用与 3D 封面一致的墨绿粗封边、象牙白纯净纸面和中性书脊阴影接管。关闭时不倒放逐页动画，只合上墨绿色前壳；3D 页片与 DOM 纸面均不绘制横线，也不包含 DOM 书签。
- `DD-20260813-004` 保存的 V1-V10 十个桌面配色候选已被用户全部否决，作为负向证据保留。无参数产品页与 production build 暂时继续使用 V2 作为运行回退值，但该状态不代表视觉验收通过。
- `DD-20260813-005` 已实现桌面空闲态的场景颜色编辑功能，可分别实时调整背景/fog、桌面、桌框、桌腿、支撑嵌板、桌垫工作面、桌垫包边、本子外壳与本子书脊/接缝。颜色只存在当前页面组件状态，支持重置和复制 JSON，刷新恢复 V2 运行回退值；当前不包含持久化主题管理。
- 通用 DOM 界面以固定版本的 `animal-island-ui` 为主要视觉参考：日期状态、桌面入口、日记外置命令和贴纸工具采用浅色 surface、胶囊圆角、厚实按压阴影与青绿/珊瑚状态；贴纸工作台使用浅薄荷点阵制作空间、暖白预览框和独立圆角控制面板。常驻品牌/“本地”顶栏已删除，3D 桌面、日记和贴纸工作台使用完整视口；项目内 `src/ui/` 只承担业务语义与主题 token。日记纸页、textarea、文件上传语义、色板、滑杆、贴纸交互层、Forge mount 和抠图 Canvas 仍由领域组件拥有。该第三方库仅用于已确认的非商业用途，署名见仓库根目录 `THIRD_PARTY_NOTICES.md`。
- 首版不提供自由走动、房间扩建或复杂装修。

### 4.2 一本按日期组织的本子

- 打开本子时默认进入当天记录。
- 打开本子默认进入阅读模式；左页保持空白并承担上一页命中，右页显示当前游标日期的正文与贴纸。
- 今天与过去日期使用同一套“编辑 → 书写 → 收笔”入口保存短文本；“阅读 / 编辑”由一个按“阅 / 读 / 编 / 辑”竖排并上下滑动的切换按钮承担，“书写 / 收笔”以“图标 / 书 / 写”或“图标 / 收 / 笔”竖排在其下方；历史贴纸继续只读。
- 日期切换使用打开的双页本子与受控的仿真纸张翻页视觉：点击空白左页进入上一页，点击右页边缘进入下一页。仿真需要表达纸张方向、弯折感和光影，但不引入真实纸张动力学、布料/软体物理引擎或第二个 WebGL Canvas。
- 正在书写且内容发生变化时，翻页、切回阅读和关闭本子会被阻止并提示先收笔；首版不自动保存草稿。
- 首版的“笔”是交互入口和视觉物件，不要求模拟真实笔迹或完整手写引擎。

### 4.3 一个贴纸制作器

- 贴纸工作台是与本子并列的独立入口，不读取日记正文草稿。
- 可以把短文本或本地图片交给固定版本的开源 `CatsJuice/sticker-forge` 制作成贴纸；不得以自研外观替代该制作核心。
- 文字可以选择内置颜色与材质；图片支持保留矩形、本地自动抠图和 Canvas 2D 手动修整。
- 图片只在当前浏览器处理，原始上传不持久化；确认后的 source PNG 与 Forge flat PNG 保存在 IndexedDB。
- 完成后可选择放到桌面或当前日期的日记；两个表面都支持放置、移动、`15°` 旋转、删除和重开恢复。
- 制作阶段可以体验 Sticker Forge 的材质与 peel；实例不持久化 peel 瞬时状态，也不运行独立 renderer。

### 4.4 一个旧痕迹入口

- 用户可以从抽屉或等价空间物件进入过去记录。
- 首版优先保存旧文字和贴纸引用。
- 首版不包含语音录制、播放或语音资源管理。

### 4.5 一盏状态明确的台灯

- 台灯提供可选的氛围状态，不是进入记录模式的强制入口。
- 灯光状态可以保存。
- 台灯不承担复杂主题商城或场景装修能力。

## 5. 首版核心验收场景

### 场景 A：留下当天记录

1. 用户打开工作台。
2. 用户打开本子，从阅读切到编辑并点击“书写”。
3. 用户输入一句话并点击“收笔”。
4. 用户重新打开页面后仍能看到当天内容。

### 场景 B：把文字或图片变成空间痕迹

1. 用户从桌面独立打开贴纸工作台，选择文字或图片。
2. 图片可保留矩形、自动抠图或手动修整。
3. 用户选择放到桌面或当前日期的日记，并在允许区域落位。
4. 贴纸可以移动、`15°` 旋转和删除；重新打开页面后仍保持表面、位置和方向。

### 场景 C：找回过去内容

1. 用户打开旧痕迹入口。
2. 用户按日期找到过去记录。
3. 用户可以看到该记录关联的贴纸或资源。

## 6. 建议数据概念

这些是首版产品概念。当前源码已实现 `DailyEntry`、图片 source asset、`StickerDefinition`、`StickerRenderAsset` 和双 surface `StickerInstance`；其余仍是预计概念：

| 概念 | 职责 |
| --- | --- |
| `DailyEntry` | 某一天的文字记录与创建、更新时间 |
| `StickerSourceAsset` | 用户确认后的图片 source PNG；不保存原始上传，首版不包含音频 |
| `StickerDefinition` | 文字或图片贴纸的 source 引用与 Forge 视觉定义 |
| `StickerRenderAsset` | Sticker Forge 确认时生成的透明 PNG Blob 与固定来源版本 |
| `StickerInstance` | 某张贴纸在桌面或指定日期日记上的位置、旋转和关联定义 |
| `DeskState` | 灯光、当前打开物件和桌面级持久状态 |

持久化数据应独立于 Three.js 对象。Three.js 场景只负责把这些状态呈现为空间物件和交互结果。

首版使用 IndexedDB 在当前浏览器本地保存数据，不建立账号、服务端数据库或云同步。清除站点数据、切换浏览器或更换设备不会自动迁移内容。

## 7. 明确非目标

- 自由走动和完整房间。
- 角色、装扮和开放社交。
- 多人协作或双人空间。
- AI 自动生成内容。
- 贴纸交易市场和数字藏品。
- 桌面上的持续 peel 物理、剥离状态持久化、撕起声音和每张贴纸独立 WebGL renderer；制作阶段可使用 Sticker Forge 自带的材质与 peel 预览。
- 远端图片抠图、云端图片处理与保存原始上传。
- 已放置图片的无损重新抠图、复杂路径编辑和图层系统。
- 语音录制、播放和语音资源管理。
- 复杂真实手写、钢笔物理或墨水模拟。
- 完整主题商城和自由家具装修。
- 账号体系、跨设备同步和云端备份。

## 8. 建议产品事件

| 事件 | 用途 |
| --- | --- |
| `desk_opened` | 观察回访频率 |
| `entry_saved` | 观察有效记录完成率 |
| `sticker_created` | 观察信息转为空间痕迹的比例 |
| `sticker_placed` | 观察核心循环完成率 |
| `past_entry_opened` | 观察用户是否回看沉积内容 |
| `desk_reset_requested` | 观察用户是否产生重置犹豫或实际重置 |

具体分析平台、隐私策略和事件字段不在本文范围内，需要在实现埋点前单独审批。

## 9. 已确认的范围决策

1. 贴纸完整 MVP 支持文字、矩形图片和本地透明轮廓抠图；`DD-20260808-002` 已批准并实现图片与双目标首切片。
2. 首版不包含语音。
3. 本子使用打开的双页模型与受控的仿真纸张翻页视觉；左页是上一页入口，右页是下一页入口。允许用 DOM/CSS 3D 透视、纸面明暗和投影表现纸张方向与弯折感，但不引入真实物理引擎或第二个 WebGL Canvas。该规则由 `DD-20260809-001` 取代原“只做轻量翻页”的限制。
4. 台灯是可选氛围状态，不是记录流程的强制入口。
5. 数据先使用 IndexedDB 本地持久化，不做账号和云同步。
6. 文字与图片贴纸制作必须真实使用固定版本的 `CatsJuice/sticker-forge`；Dear Desk 负责本地抠图、业务状态、桌面/日记放置与持久化，并保证任意时刻只有一个活跃 WebGL Canvas。
7. 贴纸工作台是独立入口；日记贴纸按日期独立于正文存在。
8. 日记左页固定留白，右页只显示当前游标日期；打开默认阅读，今天与历史正文共用“编辑 → 书写 → 收笔”流程，dirty 草稿阻止离开且不自动保存。该交互由 `DD-20260809-004` 批准并实现。

## 10. 当前实现覆盖

| 产品能力 | 当前实现状态 | 对应任务 |
| --- | --- | --- |
| 3D 桌面与本子入口 | 已实现可点击本子、DOM 等价入口，以及一个按“远处 -> 正面 -> 近处 -> 远处”循环的响应式镜头按钮；三个桌面/移动端机位集中配置，每次点击只平滑切到下一个位置。打开本子会临时聚焦近处后开盖，关闭后返回用户打开前选中的视角；若已在近处则直接开盖。镜头和本子转场中重复操作被锁定；当前 `V2.0` 程序化场景由冷灰背景、中深胡桃木无抽屉厚台面桌、蓝灰三层织物软垫和一册 `5:3` 竖向手帐组成；桌面空闲态另提供九项临时颜色编辑、重置与复制配置，刷新恢复 V2，不写入持久化。3D opening 先让前壳绕左外缘轴展开，再由 5 张视觉页片错峰循环约 9 次快速逐页翻动，清场停顿后切入 DOM 双页；完整页芯始终静止，关闭只合壳，reduced-motion 跳过逐页动画；木、布和本子外壳使用独立材质通道，桌腿与桌框也可独立改色，场景保留单投影主光和独立桌垫命中面；移动端对加长本子使用局部响应式摆位，桌下不渲染接影平面或房间地面，并在 DOM 日记拥有打开页时隐藏 3D 本子以避免重影 | 002、003、`DD-20260809-002`、`DD-20260810-002`、`DD-20260812-001`、`DD-20260812-002`、`DD-20260812-004`、`DD-20260812-005`、`DD-20260812-006`、`DD-20260813-003` 与 `DD-20260813-005` |
| 当天文字记录 | 已实现输入、校验、IndexedDB 保存与页面重开恢复；编辑器在本子打开后对齐纸页挂载 | 002、003 均已完成 |
| 过去日期、双页本子与仿真翻页 | 已实现正文/贴纸日期并集、按需跨页加载、左页空白上一页命中、右页边缘下一页、CSS 3D 翻页、重入锁和 reduced-motion；基础翻页已验收，新阅读/书写交互待验收 | `DD-20260809-001` 已完成；`DD-20260809-004` 待验收 |
| 日记阅读与书写 | 已实现默认阅读、外置阅读/编辑切换、书写/收笔、今天与历史按当前右页日期保存、失败保稿和 dirty 离开保护 | `DD-20260809-004` 待验收 |
| 独立贴纸工作台 | 已实现文字/图片切换、Forge 材质与 peel、桌面/日记双目标；日记内旧制作入口已移除 | 2026-08-08 编号 002、007 均已完成 |
| 通用 DOM 控件与主题 | 已通过 `src/ui/` 适配 `animal-island-ui`，删除旧控件外观和常驻顶栏，重写日期状态、桌面入口、日记命令、贴纸工作台、抠图工具和贴纸状态条；领域专用表面与 Canvas 保持原所有权 | `DD-20260810-002` 待验收 |
| 图片贴纸与抠图 | 已实现 PNG/JPEG/WebP 规范化、矩形、本地 U-2-Netp 自动抠图、Canvas 2D 手动修整和 source asset 持久化 | 2026-08-08 编号 002 已完成 |
| 双 surface 贴纸 | 已实现桌面 R3F 与日记 DOM 贴纸层的放置、移动、`15°` 旋转、删除、v2→v3 迁移和重开恢复 | 2026-08-08 编号 002 已完成 |
| 旧痕迹入口与台灯 | 尚未实现 | 后续任务 |

本节描述当前源码覆盖，不改变第 4、5、7 和 9 节已经批准的完整 MVP 范围。已批准但尚未实现的能力不能被视为当前产品事实。

## 11. 批准状态

用户于 2026-08-06 12:36 CST 明确回复“批准”，第 9 节五项产品范围决策全部通过。本文自此成为首版业务范围与后续任务验收边界；技术栈、数据结构细节和每段实施仍需在对应变更记录中单独批准。
