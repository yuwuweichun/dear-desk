# Action-ready evidence

`createStudyRoomShellModel` exposes `root.userData.sculptRuntime` with named floor/wall/window nodes, a floor collider, sockets for floor and west window, destruction groups for walls/window/glass, resource metrics, and an idempotent `userData.dispose`. `room-parts.json` is the corresponding assembly manifest; all room meshes are named and pointer-isolated.
