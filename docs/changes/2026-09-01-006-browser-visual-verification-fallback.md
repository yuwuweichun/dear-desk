# DD-20260901-006：浏览器视觉验证降级链路

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 待验收 |
| 类型 | 文档 |
| 创建时间 | 2026-09-01 16:35 CST |
| 最后更新 | 2026-09-01 16:40 CST |
| 当前阶段 | 已实施，待用户验收 |
| 源码基线 | Git commit b551d69815d62e6325dd352bce974d84abde5 |
| 实现提交 | 尚未创建 |
| 关联任务 | 调整 `AGENTS.md` 中浏览器视觉验证不可用时的降级顺序 |

## 1. 给阅读者的结论

浏览器视觉验证增加明确的两级降级：优先使用 `ego-browser`；不可用时改用 Codex Chrome 电脑操控；两者都不可用时继续执行现有的未覆盖记录和手动验收要求。

## 2. 用户需求

用户要求：将 `ego-browser` 不可用时的处理改为降级使用 Codex 的 Chrome 电脑操控；Chrome 也不可用时，按照原规定处理。

## 3. 当前源码事实

- `AGENTS.md` 在“HTML 文档”和“工程与验证约束”两处规定了 `ego-browser` 不可用时直接跳过视觉验证。
- 本任务只修改项目协作规则文档，不涉及业务源码、依赖、配置或持久化数据。

## 4. 目标与非目标

### 4.1 目标

- 统一规定 `ego-browser` → Codex Chrome 电脑操控 → 原有手动验收降级链路。
- 保留不可用时必须记录原因、桌面端/移动端未覆盖并告知用户手动验收的要求。

### 4.2 非目标

- 不实现或配置浏览器工具。
- 不改变视觉验收内容、产品范围或源码行为。

## 5. 方案说明

在现有两处规则中补充同样的降级顺序，避免 HTML 文档验证与前端工程验证出现不同解释。

## 6. 预计改动与影响评估

- 修改 `AGENTS.md` 的 HTML 文档视觉验证条款。
- 修改 `AGENTS.md` 的工程与验证约束条款。

### 6.1 核心数据结构变化

无。

### 6.2 上下游与跨模块影响

只影响后续任务的浏览器验收工具选择和未覆盖记录，不影响应用运行。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 两处规则措辞不一致 | 只修改一处 | 后续执行标准产生歧义 | 同步修改两处并做文本检查 |

## 8. 验证与验收

- 自动测试：不适用，纯规则文档修改。
- 构建与静态检查：运行 `git diff --check`，并检查目标文本出现次数。
- 浏览器验收：不适用。
- 持久化与恢复：不适用。
- 成功标准：两处规则均明确写出 `ego-browser` 不可用时使用 Codex Chrome 电脑操控，Chrome 也不可用时沿用原有处理。

## 9. 待确认项与决策

无。用户请求已明确授权本次文档修改。

## 10. 最终批准方案

按用户请求直接执行：仅更新 `AGENTS.md` 的两处浏览器视觉验证降级规则。

## 11. 实施记录

已实施：

- `AGENTS.md` 的 HTML 文档验证规则增加 `ego-browser` 不可用时使用 Codex Chrome 电脑操控的降级路径。
- `AGENTS.md` 的前端工程验证规则同步增加相同降级路径。
- 未修改业务源码、依赖、工程配置或持久化数据。

## 12. 验证结果

已完成：

- `git diff --check`：通过。
- `rg` 文本检查：两处规则均包含 `ego-browser` → Codex Chrome 电脑操控 → 跳过并记录未覆盖范围的顺序。
- 浏览器验收：不适用，本任务仅修改协作规则文档。

## 13. 文档同步检查

- 产品文档：不涉及。
- 架构文档：不涉及。
- 决策文档：不涉及。
- 文档入口：不涉及。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-09-01 16:35 CST | Codex | 根据用户请求创建记录并直接批准实施。 |
| 2026-09-01 16:40 CST | Codex | 完成两处规则修改并通过文档差异与文本检查，状态更新为待验收。 |
