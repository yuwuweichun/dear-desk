# DD-20260809-002：统一空间视觉为暖木纸本工坊

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 实施中 |
| 类型 | 功能 / 场景视觉 / UI 设计 |
| 创建时间 | 2026-08-09 18:46 CST |
| 最后更新 | 2026-08-10 12:50 CST |
| 当前阶段 | 内页比例、纸张层次、中缝和书签第三版已完成，等待实际截图验收 |
| 源码基线 | 批准时 Git commit `8068a6690c73571ade08a0c2536c5e045dcc4f56`；分支 `main`；工作区已有本记录、概念图及一项不属于本任务的 `package-lock.json` 修改，实施时保留 |
| 实现提交 | `f8458a1583c7c98627ac4643b6049a93ab6324a4`（与 `DD-20260809-004` 的共享 CSS、产品和架构事实联合提交） |
| 关联任务 | 用户要求弃用 `DD-20260806-006` 的 Minecraft 视觉方案，并为桌子、陈列、本子、日记页和贴纸工作台建立清晰、美观、统一的新视觉方向 |

> 用户已于 2026-08-09 20:38 CST 批准“暖木纸本工坊”程序化实施方案。Codex 将按最终执行清单直接实施；不改变业务状态、持久化模型或编号 001 的双页/翻页范围。

## 1. 给阅读者的结论

建议把 Dear Desk 的默认空间统一为 **“暖木纸本工坊”**：一张有温度但不过度写实的胡桃木书桌，搭配深苔绿书写垫、象牙纸、酒红小面积强调和旧黄铜细节；几何采用圆角、低多边形和轻微手作不规整，避免 Minecraft 的方块复刻，也避免写实资产拼盘。

这套方向直接服务产品的“私人工作台”和“使用后留下痕迹”：桌面是安静的空间容器，本子是主角，日记页像真实但清爽的活页纸，贴纸工作台像在同一张桌上展开的裁切垫与纸质工具抽屉。3D 场景和 DOM 不强行模拟同一种媒介，而是共享颜色、材质、边角、阴影、排版和物件语义，从而形成一个完整产品而不是三套界面。

本任务已完成视觉基线与首轮桌面减法，没有新增主题选择器、装修系统或日记功能。当前桌面只保留桌体、桌垫、本子和用户贴纸；桌子与桌垫方向得到用户基本认可，但纹理仍需精修，本子和内页尚未通过视觉验收，等待用户截图后继续校准。

## 2. 用户需求

### 用户原始要求

1. 确认编号 006 是否是 Minecraft 视觉改造方案，并把该方案标记为已弃用。
2. 重新设计当前空间的整体视觉。
3. 桌子的颜色、桌上陈列及物件选择、本子样式、日记页样式、工作台样式需要形成统一风格。
4. 结果需要风格化、整体一体、清晰、美观。
5. 用户当前没有明确灵感，希望 Codex 提出可审阅的设计方向。
6. 用户确认喜欢概念图中的桌子、桌垫、本子和本子内页，要求把四项分别提取为四张独立设计图。
7. 首轮源码视觉验收认为桌子和桌垫方向基本可用，但质感与纹理仍粗糙；本子与内页不符合预期，后续需要结合用户截图重新校准。
8. 用户明确要求先做减法：桌面移除本子和桌垫之外的全部摆件，后续只在新增功能确有需要时逐项增加。
9. 用户提供实际双页截图，指出内页中缝缺少清晰酒红书签；要求书签像设计稿一样成为连续、可辨识的装订细节。
10. 用户再次指出第二版仍不符合概念图：书签需要覆盖两页中缝、颜色和纹理重做、外露长度缩短；并授权从整体理解出发打磨书页比例、纸张、装订和层次，而不限于逐字修改。

### Codex 对需求的解释

- “工作台”结合当前源码解释为独立贴纸工作台，而不是编号 006 的 Minecraft crafting table。
- 本次视觉范围同时覆盖唯一 R3F 桌面、DOM 日记编辑器、贴纸工作台和桌面固定操作入口；用户生成的日记正文与贴纸内容不改变。
- “风格化”不等于增加大量装饰。建议用一致的比例、调色板、材质和留白建立辨识度，桌上固定物件控制在能说明生活痕迹但不会遮挡本子、贴纸和移动端视野的数量。
- 本任务不会把普通表单移入 WebGL，也不会为视觉改造新增第二个 Canvas；DOM 与 Three.js 继续各自承担适合的职责。
- 本次先确定单一推荐方向。若用户更喜欢其他气质，应在批准前调整本文，不先实现多个主题候选。

## 3. 当前源码事实

### 3D 桌面

- `src/scene/DeskScene.tsx` 的 `DeskContents` 是固定场景组合入口，当前拥有背景、雾、环境光、两盏方向光、桌体、桌垫、本子和用户桌面贴纸。
- 场景背景与雾均为 `#17201d`；桌体是尺寸 `[12, 0.8, 8]` 的棕色 `BoxGeometry`，桌垫是尺寸 `[8.7, 0.12, 6.25]` 的深绿色 `BoxGeometry`。两者使用纯色 `MeshStandardMaterial`，当前没有桌面纹理。
- 首轮验收后固定桌面物件只保留中央偏左本子；纸张、铅笔、笔托、杯子与台灯均已从场景移除。后续陈列必须由新增功能提出，不因填充构图而恢复。
- `Pencil` 使用低段数圆柱与圆锥，`Mug` 使用 28 段圆柱和 torus；它们与盒体本子、纸堆之间只有颜色上的弱关联，没有统一的造型规则。
- 桌垫顶面同时承担贴纸放置点击；视觉改造不能破坏 `placingDesk` 分支、世界坐标、贴纸选择或桌面 raycast。
- `CameraRig` 对桌面与本子聚焦分别维护桌面/移动端相机姿态。新增陈列必须同时避开默认镜头、本子聚焦镜头和封面打开路径。

### 本子与日记页

- `src/scene/NotebookObject.tsx` 使用盒体构成酒红封面、米色纸芯、深色书脊、金色装饰线与封面名牌；点击命中、悬停抬起、封面 pivot、打开/关闭动画均由该组件拥有。
- 本子世界位置、命中盒、封面 pivot 和开合阶段是 `CameraRig`、`JournalPanel` 与状态机的交互契约。视觉可以重做可见子网格，但不能用不带锚点的静态模型直接替换。
- `src/features/journal/JournalPanel.tsx` 以 DOM dialog 承载日期、标题、正文、贴纸层、字数、保存状态和操作。该边界符合 `DD-ADR-20260806-002` 的“普通表单使用 DOM，Three.js 是持久化状态的视觉投影”。
- `src/styles.css` 中的日记页使用半透明米白背景、Georgia/宋体、酒红保存按钮和极轻边框，已有纸本基础；但 `.journal-panel` 是完整矩形浮层，和下方 3D 书页之间缺少明确的内边距、装订、页眉和纸张纹理呼应。
- `src/sticker-workbench.css` 在 `.journal-paper` 上叠加 `JournalStickerLayer`。视觉改造必须保留归一化贴纸位置、拖动/选择层级和正文可读性。

### 贴纸工作台与全局 UI

- `src/features/stickers/StickerStudio.tsx` 的结构是左侧 Forge 预览区、右侧控制栏；文字/图片来源、抠图、材质和桌面/日记目标均属于已实现工作流。
- `src/styles.css` 的贴纸工作台使用灰米色舞台 `#cbc6bc`、浅纸色侧栏 `#f2ede3`、深绿选中态与酒红主操作。色彩与日记大体相邻，但舞台和控件呈通用工具软件语义，未表现为桌面上的裁切/装帧工具。
- `src/app/App.tsx` 在桌面稳定态显示“打开本子”和“贴纸工作台”两个 DOM 入口；`src/sticker-workbench.css` 让它们共用深色半透明按钮。全局顶栏、日期、反馈条和工作台控件需要纳入视觉 token，而不是只改 3D 模型。

### 产品、架构与工作区边界

- `docs/product/mvp.md` 明确 Dear Desk 不是以 3D 画面为卖点，而是让用户对逐渐积累的空间状态形成记忆和珍惜；主题商城、自由装修和完整房间系统属于非目标。
- `DD-ADR-20260806-002` 要求数据模型与 Three.js 对象分离；`DD-ADR-20260807-001` 要求桌面 R3F 与 Sticker Forge Canvas 不同时活跃；`DD-ADR-20260808-001` 要求日记贴纸继续由 DOM 层承载。
- `DD-20260806-006` 当前仍为 `待确认`，内容明确建议把整个场景改成 Minecraft 像素/体素风，尚未实施。用户在本任务中明确弃用该方向。
- 当前 Git 工作区存在产品、决策、场景、日记、贴纸、状态、持久化及依赖等多处其他未提交改动。本任务实施必须基于届时真实差异做局部编辑，不覆盖或回退用户现有修改。

### 合理推断

- 当前颜色已经隐约形成“深绿 + 米白 + 酒红 + 木色”，问题主要不是完全缺少审美方向，而是没有把它提升为跨 3D、纸页和工具界面的系统规则。
- 用户强调“整体视觉一体”意味着场景改造不能只替换桌面颜色；需要同时处理材质、DOM surface、排版和交互状态，但统一感不能依赖无功能陈列。
- 在未运行浏览器观察前，无法断言现有实际画面中的遮挡、比例和对齐质量；这些属于批准后实施阶段的桌面/移动端视觉验收内容。

## 4. 目标与非目标

### 4.1 目标

- 将 006 的 Minecraft 视觉方案明确标记为已弃用且未实施，保留历史设计与弃用原因。
- 为桌面、本子、日记页、贴纸工作台和全局操作入口建立一套可执行的统一视觉语言。
- 让默认空间第一眼呈现“暖木纸本工坊”：安静、温暖、风格化、清楚，具有生活痕迹但不杂乱。
- 以本子作为唯一固定桌面主角，贴纸和日记内容作为逐渐积累的个性痕迹；不使用无功能摆件填充空间。
- 保留现有本子开合、镜头、DOM 编辑、贴纸制作、桌面/日记放置、持久化和响应式行为。
- 建立可复用的视觉 token 和少量渲染配置，避免颜色散落在多个 CSS/JSX 文件中继续漂移。
- 为用户可见改动补充适当自动测试、构建检查和桌面/移动端浏览器验收。

### 4.2 非目标

- 不实现多主题切换、主题商城、自由摆放家具、完整房间、天气或昼夜系统。
- 不复用 Minecraft、Mojang 或其他品牌的原始模型、纹理和视觉标识。
- 不改变日记正文、贴纸来源、贴纸 placement、IndexedDB schema 或 Zustand 业务状态。
- 不把日记输入、贴纸工具表单或无障碍控件移入 WebGL。
- 不新增第二个 WebGL Canvas，不改变 Sticker Forge 固定集成边界。
- 不为装饰物新增交互、收藏、拖拽、购买或持久化状态。
- 不在本任务重写或扩展编号 001 的双页日记与仿真翻页行为；批准时当前 `HEAD` 已包含该功能，本任务只统一其现有 DOM 纸面视觉。
- 不为了追求“手作感”降低正文、表单标签、按钮状态和错误信息的清晰度。

## 5. 方案说明

### 5.1 视觉母题：暖木纸本工坊

核心不是复刻某个现成 IP，而是把 Dear Desk 想象成一间安静装帧师的私人桌面：物件使用了很久但维护良好，纸张、木头、布面、陶器和黄铜各有触感；整体经过轻度造型化，边角柔和、比例略夸张、细节克制。空间承载记忆，界面承载清楚的任务。

造型规则：

- 3D 固定物件采用软圆角盒体、低段数曲面和略厚的纸张层次，不使用体素方块，也不追求照片级写实。
- 材质以高粗糙度为主，允许轻微木纹、布纹、纸纤维和陶器颗粒；金属只用于极少量黄铜细节。
- 每个物件只保留一到两个辨识特征，控制小模型数量和材质数量，优先程序化几何与小型本地纹理。
- DOM surface 使用同一材质语义的二维表达：纸张靠暖白、细线和纤维噪点；布面靠深苔绿与柔和阴影；黄铜靠暖金色细边或图标强调，不使用伪 3D 重阴影。

### 5.2 调色板与视觉 token

建议以以下角色色作为第一版基线，实施时根据真实 WebGL 色彩管理和对比度微调，不把十六进制值视为未经测试的最终常量：

| 角色 | 建议色 | 用途 |
| --- | --- | --- |
| 深胡桃木 | `#6B4934` | 桌体主体、深色木边 |
| 蜂蜜木高光 | `#A7744F` | 桌沿、木纹明部、悬停暖光 |
| 深苔绿 | `#314B43` | 书写垫、本子布面、选中态 |
| 象牙纸 | `#F3E9D5` | 书页、日记页、控制栏 surface |
| 纸张阴影 | `#D8C8AD` | 分隔线、页边、禁用态 |
| 酒红印章 | `#A4484F` | 保存、危险/关键动作、日期章 |
| 旧黄铜 | `#C49A5A` | 名牌、书角、焦点环和少量强调 |
| 炭墨 | `#2D2923` | 纸面正文和深色图标 |
| 夜色墨绿 | `#17211E` | 场景背景、顶栏和深色浮层 |

CSS 预计整理为语义 token；Three.js 预计使用同一角色命名的只读场景 palette。两者共享设计角色但不强行共享运行对象。

### 5.3 桌子、构图与陈列

桌体改为暖胡桃木，顶面保留完整可贴放区域；当前大块绿色桌垫调整为深苔绿布面/皮面书写垫，弱化饱和度并增加细薄黄铜或皮革压线边缘。桌垫仍保持现有几何、点击和平面坐标契约。

首轮验收后采用极简构图：桌面固定内容只有**桌垫和中央偏左的主本子**。本子保持当前交互位置和开合骨架；桌垫继续提供可贴放区域。纸张、票据托、笔、笔托、陶杯、台灯、植物、相框、书堆、时钟及其他收藏品均不进入当前默认场景。

后续只有当新增功能需要明确空间入口或状态投影时，才通过独立任务评估增加物件。新增物件不能仅以“画面空”为理由，也不能压缩本子、用户贴纸或移动端视野。

### 5.4 本子样式

关闭状态强调“值得长期保存的私人本子”：深苔绿布纹封面、略厚圆角、象牙纸芯、酒红书签、旧黄铜名牌。名牌只使用项目现有名称/日期语义，不增加品牌装饰。书脊可以更厚，并用两条低调压线形成手工装帧感。

打开状态继续保留现有封面 pivot 和纸页锚点。可见页面使用分层纸芯和轻微页缘颜色差，不模拟昂贵的真实纸张物理。所有视觉子网格仍由 `NotebookObject` 管理；若采用圆角几何，应控制段数和阴影成本。

### 5.5 日记页样式

日记 DOM 应看起来是“落在打开本子里的可编辑纸页”，而不是盖住本子的通用弹窗：

- 象牙纸底，极轻纸纤维噪点或 CSS 生成纹理；不使用重度仿旧、污渍、撕边。
- 页顶保留日期与“今天”，但改为小号酒红日期章 + 清楚的衬线标题；正文继续使用高可读衬线字体栈。
- 正文区使用非常浅的横线或点线节奏，并确保文字行高与背景节奏对齐；贴纸层位于同一纸页坐标中。
- 使用酒红书签/页码作为方向提示，旧黄铜只用于焦点或极细分隔，不让金色成为大面积按钮色。
- 保存按钮、字数和错误信息保留明确状态。纸面主要动作使用酒红，次要动作使用透明/纸边样式。
- 桌面与移动端都保留足够可编辑高度；响应式时优先内容与触控尺寸，不强求展示完整书脊装饰。

### 5.6 贴纸工作台样式

贴纸工作台被解释为同一张桌上展开的“裁切与装帧台”：

- 左侧 Forge 预览区从普通灰底改为低饱和深苔绿裁切垫，可使用非常淡的网格/刻度，只作为方向感，不影响贴纸轮廓判断。
- 右侧控制栏使用象牙纸/浅亚麻 surface，分组像装帧工具抽屉上的纸签；控件继续保持标准 DOM 语义和足够触控尺寸。
- 来源切换、抠图工具和材质选择统一圆角、边框、悬停、选中、禁用和错误 token。深苔绿表示当前选择，酒红表示确认/关键动作，旧黄铜用于焦点和小标签。
- “放到桌面”和“放到日记”维持两个明确目标；不为统一风格改变工作流或文案层级。
- Manual Cutout 与主工作台共享同一 surface 和工具按钮规则，避免再次形成第四套界面。

### 5.7 全局界面与动效

- 顶栏继续使用夜色墨绿半透明层，但降低边框噪音；品牌字标、日期块、桌面两个入口和贴纸控制条统一 token。
- 图标继续使用当前 Lucide 线性图标，统一 stroke width；不引入另一套拟物图标。
- 动效保持短、稳、可关闭：本子悬停轻抬、按钮颜色/阴影过渡、纸页淡入。视觉改造不新增持续漂浮、粒子或环境动画。
- `prefers-reduced-motion` 继续生效，所有装饰动效不参与业务状态推进。

### 5.8 设计一致性判断标准

每个新增或修改元素都应回答四个问题：它属于木、纸、布、陶或黄铜中的哪种材质；它使用哪个角色色；它是否服务本子/贴纸/光源/操作之一；移除它是否会让界面更清楚。如果没有稳定答案，则不加入首版。

### 5.9 四张独立设计图

用户已选中概念图中的桌子、桌垫、本子和本子内页作为继续深化的对象。本轮使用原概念图作为风格参考，分别生成四张项目设计参考图，而不是直接裁剪原图：

1. **桌子设计图**：完整展示胡桃木书桌的比例、圆角桌沿、抽屉、木纹、黄铜拉手及与整体光色的关系；不摆放会遮挡结构的陈列。
2. **桌垫设计图**：展示深苔绿布面/皮面书写垫的俯视轮廓、圆角、压线、边缘厚度和克制纹理；不加入工具网格或品牌标志。
3. **本子设计图**：展示深苔绿布面硬壳本子的关闭状态、书脊、象牙纸芯、酒红书签和旧黄铜名牌，并提供能说明厚度与装帧的辅助视角。
4. **本子内页设计图**：展示打开后的双页纸面、装订中缝、象牙纸、浅横线节奏、酒红日期章/书签和清楚留白；只用少量示意文字，重点是可编辑页面结构而非日记内容。

四张图共享同一调色板、柔和低多边形造型、材质粗糙度、灯光与版式标准。它们是后续 Three.js/CSS 实现的设计参考，不是可直接贴到模型上的最终纹理，也不表示用户已经批准整个源码实施清单。

生成结果使用内置 `image_gen`，原概念图作为四次独立生成的同一风格参考；没有使用 CLI/API fallback。项目内最终文件如下：

| 设计对象 | 项目文件 | 实际结果摘要 |
| --- | --- | --- |
| 桌子 | `docs/assets/concepts/warm-paper-atelier-desk.png` | 完整胡桃木桌、三只浅抽屉、圆角厚桌沿、锥形桌腿、黄铜拉手及两个结构细节视图 |
| 桌垫 | `docs/assets/concepts/warm-paper-atelier-desk-mat.png` | 俯视深苔绿大桌垫、同色压线、圆角包边及两个材质/厚度细节视图 |
| 本子 | `docs/assets/concepts/warm-paper-atelier-notebook.png` | 关闭状态布面硬壳本子、黄铜名牌、象牙纸芯、酒红书签及两个装帧细节视图 |
| 本子内页 | `docs/assets/concepts/warm-paper-atelier-journal-pages.png` | 打开双页、浅横线、中缝酒红书签、纸张层次及两个装订/纸纹细节视图 |

四次最终提示词均使用 `stylized-concept` 分类，并分别要求：完整展示桌子结构且不摆放陈列；桌垫无网格且保留大面积可用面；本子保持苔绿布面、象牙纸芯、酒红书签和无文字黄铜名牌；内页保持双页留白、浅横线和无正文。共同排除了 Minecraft、体素、像素画、品牌文字、水印、重度仿旧和无关物件。

### 5.10 程序化实现策略与预计还原度

本方案建议继续使用现有 React Three Fiber / Three.js 场景，以**程序化几何为主、轻量程序纹理为辅、DOM 承载可编辑纸面**。四张设计图是造型、比例、材质和光色的视觉对照，不作为整张运行时贴图直接覆盖模型，也不要求先制作 Blender / glTF 资产。

当前项目使用 `three@^0.185.1` 与 `@react-three/fiber@^9.7.0`，已有能力足够完成首版，不预计新增 `@react-three/drei`：

| 对象 | 预计实现 | 保留的现有契约 | 预计还原度 |
| --- | --- | --- | --- |
| 桌子 | 使用 Three.js addon 的 `RoundedBoxGeometry` 或本地圆角/挤出几何组成桌面、抽屉和锥形桌腿；圆柱/球体组成黄铜拉手；小尺寸 `CanvasTexture` 生成克制木纹 | 现有桌面世界范围、相机安全区和贴纸可视区域 | 造型、配色和整体材质约 `85%–90%`；设计图中的细密木纹与摄影级柔光约 `70%–80%` |
| 桌垫 | 圆角 `Shape` + `ExtrudeGeometry` 或低段数圆角盒体；压线由内圈曲线或程序纹理表达；视觉网格与简单命中面分离 | `placingDesk` 点击、raycast 和贴纸世界坐标保持不变 | 轮廓、厚度、压线和苔绿色约 `90%` |
| 本子 | 保留现有 `group`、透明命中盒、封面 pivot 和 `NotebookPhase`，只替换可见子网格；圆角布面封面、分层纸芯、书脊压线、书签和黄铜名牌均程序化构成 | 点击、悬停抬起、开合角度、镜头锚点和阶段回调保持不变 | 关闭状态约 `85%–90%`；布纹微观质感约 `75%–85%` |
| 本子内页 / 日记 | Three.js 表达书芯、页缘和装订结构；实际日期、标题、正文、保存状态与贴纸继续由 DOM/CSS 表达，使用 CSS 渐变、浅横线和轻纸纤维 | 可编辑性、无障碍、贴纸归一化坐标和保存流程保持不变 | 批准时双页功能已存在；本任务预计将其视觉统一度提升到约 `85%–90%` |
| 灯光与极简构图 | 保留环境光和两盏方向光，不增加可见灯具或其他固定摆件 | 默认镜头、本子聚焦、本子开合和桌面贴纸均不受装饰遮挡 | 第一眼构图与色温约 `80%–90%`；材质细节仍需继续校准 |

这里的百分比是对**视觉语言、轮廓、比例、颜色和材质关系**的工程预估，不是像素级相似度承诺。设计图采用概念设计布光和理想化材质，真实应用还受固定相机、交互命中区、移动端裁切、实时渲染性能和 DOM 可读性约束，因此不应承诺 100% 或摄影级复刻。

在无需外部 3D 建模资产的首版中，可以达到“用户第一眼认为就是同一套设计”的程度。若后续目标变为近乎静帧级复刻，则需要另行评估 Blender / glTF、烘焙 PBR/法线纹理、模型锚点重建和更高 GPU 成本；这不属于当前推荐路径，也不是本次视觉统一的必要条件。

实施时建议用四张设计图逐项做桌面与移动端截图对照，优先校准大轮廓、比例、色温和层级，再调整纹理细节。编号 001 的双页日记在批准时已成为当前源码能力；本方案只为其换肤，不借视觉改造调整翻页状态机或导航边界。

## 6. 预计改动与影响评估

| 预计模块 | 预计责任 |
| --- | --- |
| 新的视觉 token / palette 模块 | 统一 CSS 语义色、边角、阴影、间距，并为场景提供只读角色色；不保存 Three.js 实例 |
| `src/scene/DeskScene.tsx` 及必要的固定场景子组件 | 重做桌体、桌垫与全局光色，固定桌面只保留本子；保留桌面点击、贴纸和相机调用链 |
| `src/scene/NotebookObject.tsx` | 在保留 group、命中盒、pivot、阶段回调和世界锚点的前提下重做布面本子与纸芯视觉 |
| `src/features/journal/JournalPanel.tsx`、`JournalStickerLayer.tsx` 与样式 | 调整纸页语义、页眉与 surface 层次；保留表单、贴纸坐标和保存行为 |
| `src/features/stickers/StickerStudio.tsx`、`ManualCutoutEditor.tsx` 与样式 | 将预览区、控制栏和工具状态统一为裁切/装帧台；不改变 Forge、抠图和 placement 行为 |
| `src/app/App.tsx` 与全局样式 | 统一顶栏、日期、入口按钮、反馈条和响应式 token |
| 相关测试 | 锁定本子交互骨架、桌面放置、日记表单、工作台目标动作与可访问状态；避免为纯像素值建立脆弱快照 |
| `docs/product/mvp.md`、相关架构文档、本文与文档入口 | 实施后把批准并真实完成的视觉事实、源码地图、偏差和验证结果同步回写 |

实施前需重新读取 Git 差异，尤其是当前已修改的 `DeskScene.tsx`、`JournalPanel.tsx`、`StickerStudio.tsx`、`App.tsx`、`styles.css` 与 `sticker-workbench.css`。所有编辑应以当前文件为基线进行小范围补丁，不从旧提交整体覆盖。

### 6.1 核心数据结构变化

预计不改变领域模型、Zustand 状态、repository 接口或 IndexedDB schema。新增内容只属于样式和场景渲染配置：

```text
approved visual roles
  -> CSS custom properties (DOM surfaces and controls)
  -> readonly scene palette / material settings (R3F fixed objects)
  -> current components render the same application state

DailyEntry / StickerDefinition / StickerInstance / NotebookPhase
  -> unchanged
  -> existing persistence and state transitions remain authoritative
```

如果实施中发现需要为灯光、陈列或主题增加持久化字段，说明范围已经从“固定默认视觉”扩大到“可变空间状态”，必须更新本文并重新获得批准。

### 6.2 上下游与跨模块影响

- `DeskContents` 是固定场景上游组合点；桌垫仍是桌面贴纸放置事件的下游命中面。
- `NotebookObject` 的视觉子网格受 `NotebookPhase` 驱动，`CameraRig` 与 `JournalPanel` 依赖其位置和开合结果；改模型时必须保持交互骨架。
- `JournalPanel` 的纸面变化会影响 `JournalStickerLayer` 可用区域、textarea 行高、贴纸遮挡和移动端键盘空间。
- Sticker Forge 自己拥有制作 Canvas；外层视觉只能调整挂载容器和 DOM 控制栏，不能访问或重绘上游私有 renderer。
- 颜色和 surface token 会影响全局顶栏、错误提示、选择态、焦点环、禁用态与对比度，需做键盘和可访问性回归。
- 新增纹理或圆角几何会影响构建体积、材质数量、阴影和 GPU；程序化/CSS 纹理优先，必要的位图需本地存放并记录来源。
- 方案起草时编号 001 尚待确认，但批准时基线已通过 `d28772f` 实现双页与仿真翻页，并由 `8068a66` 记录验收。本任务将现有双页结构视为源码事实，只调整视觉，不改变其数据、导航或交互。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 风格变成“复古咖啡馆”而非私人工作台 | 大量使用仿旧、装饰字体、棕黄滤镜和无功能摆件 | 信息模糊、产品辨识度被装饰吞没 | 纸张保持干净、正文使用可读字体；当前固定桌面只保留本子和桌垫 |
| DOM 与 3D 仍像两层贴图 | 只改颜色，不核对书页锚点、内边距和层次 | 日记面板继续漂浮，统一感不足 | 桌面/聚焦镜头同时验收；以本子可见边缘、纸面边距和阴影建立衔接 |
| 后续功能物件遮挡入口或贴纸 | 新增空间入口进入本子动画或桌面放置区 | 点击失败、移动端裁切、用户贴纸空间缩小 | 后续逐任务评估安全区；当前不保留任何无功能摆件 |
| 视觉 token 改动引起对比度回归 | 全局替换颜色但未检查状态和暗/亮 surface | 标签、焦点、错误或禁用态不清楚 | 按组件状态核对，对正文和控件执行 WCAG 对比检查；保留旧 token 的可回退补丁 |
| 本子交互回归 | 重构可见网格时改变 pivot、命中盒或位置 | 无法打开、镜头错位、DOM 对不齐 | 保留交互骨架与世界锚点；新增契约测试并完整回归开合流程 |
| 贴纸坐标或可见性回归 | 改变桌垫/纸页命中面或 overlay 尺寸 | 已有贴纸位置漂移或无法放置 | 不改 placement 数据；锁定几何与归一化坐标；重开恢复实测 |
| 资产和 GPU 预算膨胀 | 使用大纹理、高段数圆角或多盏投影灯 | 首屏变慢、移动端掉帧 | 程序化/CSS 纹理优先；限制材质和灯光；记录构建资产增量 |
| 覆盖并行未提交改动 | 从旧基线重写当前组件 | 其他任务成果丢失 | 实施前逐文件审阅 diff，只做局部补丁；发现范围冲突立即暂停 |
| 与编号 001 双页方案产生冲突 | 两项同时改本子、日记布局和点击区域 | 重复实现或交互边界不清 | 本任务不实现翻页；若 001 先获批准，以最新批准方案重新核对视觉结构 |

回退不迁移或删除用户数据。若新场景出现严重问题，可按组件恢复旧材质/几何和旧 CSS token；若纹理影响性能，优先关闭程序纹理并保留几何、统一色彩和纸面样式。

## 8. 验证与验收

- 自动测试：运行现有日记、贴纸、状态、持久化和本子转场测试；为交互骨架与关键可访问状态补充必要测试，不为纯装饰像素建立脆弱快照。
- 构建与静态检查：运行 `npm run lint`、`npm run test`、`npm run build`、`node scripts/check-doc-references.mjs` 与 `git diff --check`。
- 浏览器验收：`ego-browser` 可用时，在桌面 `1440 × 900` 与移动端 `390 × 844` 核对默认桌面、本子聚焦/开合、日记编辑、贴纸工作台、手动抠图和所有反馈状态；若不可用，明确记录桌面与移动端未覆盖并交由用户手动验收。
- 视觉验收：桌面、本子、纸页和工作台能被描述为同一个“暖木纸本工坊”；主角明确、固定陈列不杂乱、文字和控制清楚，没有 Minecraft 体素或品牌资产残留。
- 交互验收：桌面放置/选择贴纸、本子打开/关闭、日记输入/保存、日记贴纸操作、文字/图片制作、抠图和两个放置目标全部可用。
- 响应式验收：移动端无横向溢出、关键按钮触控尺寸足够、键盘弹出后正文仍可编辑；装饰物不遮挡入口。
- 可访问性验收：键盘焦点可见，标题/表单/对话框语义不退化，正文、标签、错误、禁用和选中态保持可辨识对比度。
- 持久化与恢复：页面重新打开后日记、桌面贴纸和日记贴纸不丢失、不漂移；固定视觉正常重建，不新增数据库字段。
- 成功标准：用户无需看源码即可感知一套完整且克制的空间个性，同时所有现有主流程与本地数据保持可靠。

## 9. 待确认项与决策

### 决策 1：是否采用“暖木纸本工坊”作为唯一实施方向

**建议：采用。** 它延续当前已有的深绿、木色、米白和酒红基础，改造成本可控，也最贴近“私人工作台”和“痕迹积累”的产品核心。

若用户希望更明亮、现代或奇幻，需要在批准前重新定义完整母题，而不是实施中混入第二套风格。

### 决策 2：固定陈列范围

**首轮验收修正：只保留桌垫和本子。** 黄铜蘑菇灯、陶杯、纸托和书写工具均已移除；后续只因新增功能增加物件。

### 决策 3：纸张纹理强度

**建议：非常轻，只在大面积空白处可感知。** 不使用明显噪点、污渍、折痕和做旧边缘；文字与贴纸始终优先。

### 决策 4：实施切片

**建议：本任务一次批准后分四个可运行小步实施：视觉 token 与桌面基线；本子与日记页；贴纸工作台；全流程验证与文档回写。** 每一步都以现有功能可运行为门槛，不拆成多个需要重复审批的新目标。

## 10. 最终批准方案

已批准。最终执行清单为：

1. 固定“暖木纸本工坊”母题、角色色、材质、造型与陈列安全区。
2. 整理 CSS token 与场景 palette，不改变业务状态或持久化。
3. 局部改造桌体、桌垫和光色，固定桌面只保留本子，并保留桌面贴纸命中面。
4. 保留 `NotebookObject` 交互骨架，改造布面封面、纸芯、书签和名牌。
5. 将日记页对齐为干净纸本 surface，保留正文、贴纸层、保存与无障碍语义。
6. 将 Sticker Studio 与 Manual Cutout 统一为裁切垫 + 纸质工具栏，保留全部工作流。
7. 运行自动检查、桌面/移动浏览器验收、持久化恢复和文档引用检查。
8. 对照批准方案与实际 Git 差异回写产品、架构、本文和文档入口，状态更新为 `待验收`。

## 11. 实施记录

已按批准清单完成源码实施，未修改领域模型、Zustand 状态、IndexedDB schema、repository 接口、Sticker Forge 集成或第三方依赖：

1. 新增 `src/scene/RoundedBox.tsx`，用 Three.js addon `RoundedBoxGeometry` 提供可复用圆角盒体，并在几何替换或卸载时显式 `dispose()`。
2. 新增 `src/scene/scene-visuals.ts`，集中维护暖木纸本工坊场景角色色，并用确定性 Canvas 绘制轻量木纹、布纹 `CanvasTexture`；纹理由使用组件 memo 化并在卸载时释放。
3. `src/scene/DeskScene.tsx` 将单盒桌体重组为圆角胡桃木桌面、前沿、三只抽屉、黄铜拉手与四条锥形桌腿；桌垫改为圆角深苔绿面与四边压线；固定桌面只保留本子。原桌垫 placement 事件、贴纸投影、相机与 Canvas 生命周期保持不变。
4. `src/scene/NotebookObject.tsx` 保留原 `group`、透明命中盒、封面 pivot、阶段机和开合动画，替换为圆角布面封底/封面、分层纸芯、深色书脊、酒红书签、页线、黄铜名牌与压线。
5. `src/styles.css` 建立木、布、纸、酒红、黄铜与暖阴影语义 token；统一顶栏、日期块、入口按钮、双页封皮、纸纤维、横线、中缝书签、翻页正反面、保存动作和 Sticker Studio。Forge 舞台改为深苔绿裁切垫与两级网格，右栏改为象牙纸工具面。
6. `src/sticker-workbench.css` 将桌面入口、来源切换、上传、目标动作、日记贴纸选择和 Manual Cutout 统一为相同圆角、纸面、苔绿裁切垫与酒红动作规则。
7. 实施没有改动 `JournalPanel.tsx`、`JournalPage.tsx`、`PageTurnSheet.tsx`、`StickerStudio.tsx` 或 `ManualCutoutEditor.tsx` 的结构和行为；现有语义结构已足够，CSS 换肤降低了对并行功能源码的侵入。
8. 同步 `docs/product/mvp.md`、`docs/architecture/system-overview.html` 的当前事实/源码地图、`docs/index.html` 状态与本文。HTML 只做局部事实和链接更新，没有改变结构、样式或脚本。

首轮验收修正：根据用户 2026-08-09 23:06 CST 的明确要求，桌面固定陈列收缩为桌子、桌垫和本子三项；纸托/票据/黄铜夹、笔/笔托、陶杯与黄铜蘑菇灯从场景源码移除。桌体自身的抽屉、桌腿和拉手继续保留，不视为桌面摆件。本子与内页暂不凭推断继续调整，等待用户截图后再做针对性校准。

截图修正第一版把原中缝中只有约 6% 宽度的弱酒红色线改为中缝伪元素，但用户第二张截图确认它仍像装饰条，不像概念图中的实体书签。第二版在 `JournalPanel.tsx` 增加独立、无交互的 `.journal-bookmark`：桌面端宽 16px，从书页顶部跨过整本书并伸出书本/状态区底部 34px，以织物横纹、侧边暗部和 V 形燕尾表现实体感；移动端缩为 10px、伸出 18px。`NotebookObject.tsx` 的 3D 酒红标签同时从右侧移回本子中线，避免打开状态出现两条错位书签。该修正不改变日期、贴纸、保存或翻页行为。

第三版不再只修书签参数，而是整体重做内页层次：`JournalPanel.tsx` 增加 `.journal-book-stage`，让书签长度相对书本而非整个 panel 计算，并保持并行任务 004 已实现的阅读/编辑状态与保存逻辑原样不动。`src/styles.css` 将桌面本子宽度上限从 980px 提升到 1180px；封皮边框从 11px 加厚到 14px并使用更大的 30px 圆角；左右页增加不对称外圆角、外缘页堆条纹、内折阴影和底缘纸张阴影。原每 5px 重复的规则纸纹改为 17px / 29px 的低对比度双向纤维，书写横线统一为页面级 50px 节奏。书签宽度从 16px 增为与中缝相同的 30px，颜色改为深氧化酒红，纹理改为细密横纵交织；外露长度从相对 panel 的约 54px 收短为相对书本的 26px。移动端中缝/书签均为 14px，外露 14px。

实施偏差：方案原本把编号 001 视为未批准，但当前批准基线已经实现并验收双页翻页。本任务因此直接统一现有双页视觉，不再以单页为过渡，也没有修改 001 的导航、加载或翻页状态机。批准时已存在的 `package-lock.json` WSL 平台元数据差异未被本任务修改或回退。

## 12. 验证结果

- 已确认 `DD-20260806-006` 是整个桌面 Minecraft 像素/体素视觉方案，状态原为 `待确认`，实施记录明确写明尚未实施。
- 已读取 `docs/product/mvp.md`、相关架构/决策、当前 `DeskScene.tsx`、`NotebookObject.tsx`、`JournalPanel.tsx`、`StickerStudio.tsx`、`App.tsx` 和样式文件。
- 已检查 Git 基线与工作区状态，确认存在大量其他任务未提交改动；本轮没有覆盖这些改动。
- `npm run lint` 已通过。
- `npm run build` 已通过；Vite 仍报告既有主包与抠图 WASM 超过建议尺寸，本任务未新增依赖或新的大型运行时资产。
- 首次 `npm run test` 在用例导入前失败，原因是当前 WSL 进程继承 Windows 临时目录并无法创建 `/mnt/c/Users/18379/AppData/Local/Temp/.../client`；以 `TMPDIR=/tmp TMP=/tmp TEMP=/tmp npm run test` 重跑后，10 个测试文件、30 个用例全部通过。
- 按 React 最佳实践检查新增/修改 TSX：组件未嵌套定义，程序纹理与几何均 memo 化/显式释放，静态陈列配置已提升到模块级，Hooks 依赖完整，没有新增不必要状态订阅或包体依赖。
- 当前环境中 `ego-browser` 不可用，按项目规则跳过浏览器视觉验收。桌面 `1440 × 900`、移动端 `390 × 844` 的实际构图、遮挡、灯光亮度、Forge 舞台和完整交互仍需用户手动验收；本文不把这些范围写为通过。
- 最终以 `TMPDIR=/tmp TMP=/tmp TEMP=/tmp npm run check` 连续执行 lint、测试、构建和文档检查并通过；随后 `git diff --check` 与独立 `node scripts/check-doc-references.mjs` 均通过。
- 桌面减法完成后再次以相同 `/tmp` 环境运行 `npm run check`：lint、10 个测试文件/30 个用例、生产构建和文档检查全部通过；`git diff --check` 通过，`DeskScene.tsx` 中不存在被移除摆件或相关 point light / sphere / torus / cone 几何的残留引用。
- 酒红书签修正后再次运行相同完整检查：lint、10 个测试文件/30 个用例、生产构建、文档引用与 `git diff --check` 全部通过；浏览器视觉仍需用户查看实际中缝宽度与对比度。
- 第二版独立实体书签与 3D 中线对齐完成后再次运行相同完整检查：lint、10 个测试文件/30 个用例、生产构建、文档引用与差异检查全部通过；由于 `ego-browser` 不可用，伸出长度、V 形缺口和概念图相似度仍以用户实际截图验收为准。
- 第三版整体内页打磨后运行 `TMPDIR=/tmp TMP=/tmp TEMP=/tmp npm run check`：lint、10 个测试文件/33 个用例、生产构建和文档检查全部通过；新增的 3 个用例来自并行任务 004 的阅读/编辑实现，本轮视觉容器调整没有破坏它们。`git diff --check` 与独立文档引用检查通过。`ego-browser` 仍不可用，实际宽高比例、页缘层次和书签质感等待用户截图验收。
- 已使用内置 `image_gen` 生成一张仅供方案审阅的 16:9 概念图：`/mnt/c/Users/18379/.codex/generated_images/019fe60b-a106-7391-a09d-cbbf64b5cfe5/exec-08688016-2f65-4f62-910a-30d558df0a27.png`。图中验证了胡桃木、苔绿布面、象牙纸、酒红与旧黄铜可以跨桌面、日记和裁切工作台形成一致层级；左侧常驻工具栏、英文示例正文和蘑菇贴纸只是生成图构图内容，不属于预计实现。
- 概念图采用 `ui-mockup` 用例，提示词要求主桌面占约三分之二、右侧展示日记与贴纸工作台细节，并明确排除 Minecraft、体素、像素、杂乱 cottagecore、蒸汽朋克、重度做旧和不可读 UI。图片为预览，不复制到项目，也不会被源码引用。
- 已完成四次独立内置 imagegen 生成并逐张视觉检查。四张图均为 `1672 × 941` RGB PNG，已复制到 `docs/assets/concepts/`；文件存在、格式可识别且 SHA-256 已记录于本轮终端输出。
- 桌子图没有桌垫和陈列，结构完整；桌垫图没有网格和工具；本子图没有文字/标志；内页图没有正文、贴纸或工具栏。四张图没有 Minecraft、体素、像素或水印内容。
- `git diff --check` 已通过，没有发现本轮已跟踪文档的空白错误。
- `node scripts/check-doc-references.mjs` 已执行但全库检查未通过；失败项全部来自本轮开始前已存在且不属于本任务的 `docs/changes/2026-08-08-001-require-bun-html-docs.md`，该文件缺少“文档信息”“1. 给阅读者的结论”“文档同步检查”“审阅记录”章节。本轮不擅自修改该并行任务，相关全库门槛仍待其所有者补齐。
- 四张设计图落盘后再次运行 `node scripts/check-doc-references.mjs`，全库文档检查已通过；说明上述并行文档已在本轮期间由其所有者补齐，本任务没有修改该文件。

## 13. 文档同步检查

- 产品文档：已在 `docs/product/mvp.md` 把暖木纸本工坊写入当前桌面事实与实现覆盖；没有改变产品流程或非目标。
- 架构文档：已在 `docs/architecture/system-overview.html` 补充程序化视觉组件分工与源码地图；数据所有权、DOM/R3F、单 Canvas 和 local-first 边界不变。
- 决策文档：不新增；本任务没有引入新的长期架构决策。
- 文档入口：已将 `docs/index.html` 中本任务从待确认更新为待验收，并保留 006 已取消事实。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-08-09 18:46 CST | 用户 | 要求弃用编号 006 的 Minecraft 视觉方案，并为桌面、本子、日记页和贴纸工作台提出统一、清晰、美观的新视觉方向。 |
| 2026-08-09 18:46 CST | Codex | 完成只读事实核对，创建“暖木纸本工坊”待确认方案；除已授权的 006 弃用标记外，尚未实施业务改动。 |
| 2026-08-09 18:52 CST | Codex | 完成 006 与文档入口的弃用状态同步，并使用内置 imagegen 生成仅供审阅的统一视觉概念图；未把图片加入项目资产。 |
| 2026-08-09 20:02 CST | 用户 | 确认喜欢概念图中的桌子、桌垫、本子和本子内页，要求分别生成四张独立设计图。 |
| 2026-08-09 20:02 CST | Codex | 将四张设计图纳入同一任务记录；使用原概念图作为风格参考，生成后保存到项目文档资产目录，不修改业务源码。 |
| 2026-08-09 20:07 CST | Codex | 完成四张独立设计图生成、视觉检查、项目内持久化和文件格式/hash 核对；四张图仅作为已选视觉方向的设计参考。 |
| 2026-08-09 20:11 CST | 用户 | 接受四张独立设计图，并要求把图片放入项目内的恰当目录。 |
| 2026-08-09 20:11 CST | Codex | 确认四张图已位于 `docs/assets/concepts/`；它们属于设计文档参考，不进入运行时 `public/` 或源码资产目录。整体视觉源码实施仍待单独批准。 |
| 2026-08-09 20:20 CST | 用户 | 询问整体视觉是否通过程序化对象创建、预计还原效果，以及能否达到与设计图相近的视觉表现。 |
| 2026-08-09 20:20 CST | Codex | 核对当前 Three.js / R3F 版本和桌面、本子交互骨架，补充程序化几何、轻量纹理、DOM 纸面分工与分对象还原度预估；未修改业务源码。 |
| 2026-08-09 20:38 CST | 用户 | 明确批准“暖木纸本工坊”及程序化实现方案。 |
| 2026-08-09 20:38 CST | Codex | 将任务状态更新为已批准，开始按最终执行清单复核当前基线；确认不把编号 001 的双页/翻页交互并入本任务。 |
| 2026-08-09 20:38 CST | Codex | 基线复核发现方案起草后编号 001 已由并行提交实现并验收；该变化不扩大本任务业务范围，实施仅统一现有双页视觉，不改翻页逻辑。任务进入实施中。 |
| 2026-08-09 20:52 CST | Codex | 完成程序化场景、本子与 DOM 工作面的统一视觉实施；lint、30 个测试、生产构建、文档引用和差异检查通过。因 `ego-browser` 不可用，桌面/移动视觉留待用户手验，任务更新为待验收。 |
| 2026-08-09 23:06 CST | 用户 | 首轮验收认为桌子、桌垫方向基本可用但纹理粗糙，本子和内页不符合预期；明确要求先移除桌面上除本子、桌垫之外的所有摆件。 |
| 2026-08-09 23:06 CST | Codex | 将任务恢复为实施中；先执行已明确的桌面减法，本子与内页等待用户截图后针对性调整，不自行猜测新的造型方向。 |
| 2026-08-09 23:12 CST | Codex | 已从场景源码移除纸托、票据、笔、笔托、陶杯、蘑菇灯及其专用几何/灯光注册；完整自动检查通过。任务保持实施中，等待用户提供本子与内页截图。 |
| 2026-08-09 23:25 CST | 用户 | 提供实际双页截图，指出中缝缺少明确酒红书签，并提出左页留白、历史可编辑和两级模式控件的新交互要求。 |
| 2026-08-09 23:25 CST | Codex | 在本任务中补强中缝酒红书签；会改变日记状态与保存范围的交互需求转入 `DD-20260809-004` 独立审批。 |
| 2026-08-09 23:35 CST | 用户 | 第二张实际截图显示第一版书签仍与概念图不符。 |
| 2026-08-09 23:35 CST | Codex | 对照概念图确认第一版误做成中缝装饰条；改为跨越整本书并伸出底部的独立织带，同时将 3D 标签移回中线。 |
| 2026-08-09 23:40 CST | Codex | 第二版书签完成全量自动回归；等待用户从实际画面确认与概念图的相似度。 |
| 2026-08-09 23:54 CST | 用户 | 提供最新实际图与概念图对照，指出书签宽度、颜色、纹理、长度仍不正确，并要求整体打磨书本内页实现。 |
| 2026-08-09 23:54 CST | Codex | 对照确认当前本子过窄过高、页面矩形化、纸纹规则、页堆和封皮层次不足；开始整体视觉修正。发现工作区已出现任务 004 的并行交互实现，本轮只做局部视觉结构和 CSS，不覆盖其状态与保存逻辑。 |
| 2026-08-09 23:58 CST | Codex | 完成第三版整体打磨：扩宽书本、加厚封皮、重做页缘/页堆/纸纤维/横线/中缝和深酒红书签；并行阅读/编辑功能的 33 个测试及全量检查通过，等待截图验收。 |
| 2026-08-10 12:50 CST | Codex | 用户授权梳理并提交当前工作区；本任务与 `DD-20260809-004` 因共享 CSS 和事实文档联合提交为 `f8458a1`。提交不代表视觉验收，本任务继续保持实施中。 |
