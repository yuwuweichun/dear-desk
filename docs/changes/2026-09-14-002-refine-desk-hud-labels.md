# DD-20260914-002：调整桌面 HUD 操作顺序与文案

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已完成 |
| 类型 | 功能 |
| 创建时间 | 2026-09-14 00:00 CST |
| 最后更新 | 2026-09-14 13:30 CST |
| 当前阶段 | 用户已验收，已授权创建提交 |
| 源码基线 | 当前工作区 `149bf89` |
| 实现提交 | 本次提交 |
| 关联任务 | 调整桌面 HUD 底部入口顺序、历史入口文案与视角按钮文案 |

> 用户已于 2026-09-14 明确批准本方案；当前记录进入待验收状态。

## 1. 给阅读者的结论

桌面底部操作栏将把核心功能“打开本子”移动到最左侧，并把历史入口从“旧痕迹”改为四字文案“旧时日记”。镜头按钮统一使用带“视角”的表达，当前状态显示“正面视角”“近处视角”或“远处视角”。

本次只调整常驻 DOM 控件的顺序、可见文案和对应测试断言，不改变按钮行为、状态机、数据结构或 3D 场景。

## 2. 用户需求

- 用户原始要求：确认四周环绕的 UI 可称为 HUD；将“打开本子”移到最左侧；将“旧痕迹”改成“旧时日记”或其他四字表达；将“正面”改为“正面视角”，并使用“近处视角”“远处视角”表达切换。
- Codex 解释：本次只处理截图中底部常驻操作栏，不改右上角工具栈，也不改功能语义。

## 3. 当前源码事实

- `src/app/App.tsx` 的 `desk-actions` 原顺序为镜头按钮、打开本子、旧痕迹、编辑铭牌、装饰工坊；现已调整为打开本子、旧时日记、编辑铭牌、装饰工坊、视角按钮。
- 同一文件中的 `cameraPresetLabels` 原为“远处 / 正面 / 近处”，现为“远处视角 / 正面视角 / 近处视角”，按钮的 aria-label、title 和可见文本均由该映射派生。
- `src/features/history/PastTracesPanel.tsx`、`src/app/App.tsx` 的降级入口和相关测试已统一使用“旧时日记”。
- `src/sticker-workbench.css` 负责 `desk-actions` 的底部布局；当前没有发现必须配合修改的专用排序逻辑。
- `src/app/App.test.tsx`、`src/features/history/PastTracesPanel.test.tsx` 和 `src/state/app-store.test.ts` 覆盖相关文案或镜头循环行为。

## 4. 目标与非目标

### 4.1 目标

- 将“打开本子”放在桌面底部操作栏最左侧。
- 将历史入口及其面板标题、状态和辅助文案统一改为“旧时日记”语义。
- 将固定机位按钮的三种可见名称统一为“远处视角 / 正面视角 / 近处视角”。
- 更新受影响的自动化测试和架构事实文案。

### 4.2 非目标

- 不改变按钮点击逻辑、相机循环顺序或禁用条件。
- 不改变历史数据、路由、持久化结构、Three.js 场景和右上角工具栈。
- 不新增 HUD 组件或第三方依赖。

## 5. 方案说明

复用现有 `desk-actions` DOM 顺序和 `cameraPresetLabels` 映射，直接调整 JSX 顺序与字符串。历史面板内的“旧痕迹”仅作为用户可见功能名统一替换为“旧时日记”；内部状态名、文件名和数据概念保持不变，避免扩大范围。

## 6. 预计改动与影响评估

- 修改 `src/app/App.tsx`：调整五个底部按钮顺序；更新历史入口文案；更新相机标签映射。
- 修改 `src/features/history/PastTracesPanel.tsx`：更新面板标题、关闭按钮、加载/空态/错误/日期入口的用户可见文案。
- 修改 `src/app/App.test.tsx` 与 `src/features/history/PastTracesPanel.test.tsx`：同步可见名称和可访问名称断言。
- 修改 `docs/architecture/system-overview.html` 与必要的产品事实文字：同步当前入口名称与顺序事实。

### 6.1 核心数据结构变化

不变。相机 preset ID、历史状态字段、IndexedDB 表和持久化内容均不变。

### 6.2 上下游与跨模块影响

只影响 DOM 文案、可访问名称和底部按钮排列；点击处理函数、状态机、音效触发和场景交互不变。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 测试依赖旧可访问名称 | 文案更新后旧断言仍查找“旧痕迹” | 测试失败但功能不受影响 | 同步更新相关断言；保留内部状态与文件名 |
| 小屏按钮空间不足 | 顺序变化后出现换行或溢出 | 底部操作栏可读性下降 | 复用现有响应式规则并执行构建/测试；不增加新布局规则 |

## 8. 验证与验收

- 自动测试：运行受影响的 App、历史面板和 store 测试。
- 构建与静态检查：运行项目既有 lint/build 命令。
- 浏览器验收：本次用户未明确要求浏览器验收，默认不执行。
- 持久化与恢复：不适用，数据结构不变。
- 成功标准：底部第一项为“打开本子”；历史入口显示“旧时日记”；镜头三态显示带“视角”的名称；相关测试和构建通过。

## 9. 待确认项与决策

- 建议采用“旧时日记”作为四字历史入口文案；若用户希望更偏情绪化，可改为“往日手记”，但会改变本次验收文字。
- 其余范围无待确认项。

## 10. 最终批准方案

2026-09-14 用户明确批准；执行第 6 节的最小修改清单。

## 11. 实施记录

已完成：

- 调整 `src/app/App.tsx` 的底部 HUD 顺序，将“打开本子”移到最左侧。
- 将历史入口、历史面板状态和可访问名称统一为“旧时日记”。
- 将固定机位标签统一为“远处视角 / 正面视角 / 近处视角”。
- 更新相关测试、`docs/product/mvp.md` 和 `docs/architecture/system-overview.html`。

## 12. 验证结果

已验证：

- `npm test -- --run src/app/App.test.tsx src/features/history/PastTracesPanel.test.tsx src/state/app-store.test.ts`：3 个测试文件、33 个测试通过。
- `npm run lint`：通过。
- `npm run build`：在受限环境外通过；受限环境内因 Vite `spawn EPERM` 无法加载配置。
- `node scripts/check-doc-references.mjs`：通过。
- 未执行浏览器验收，因本次请求未明确要求；需用户在实际页面确认视觉间距。

## 13. 文档同步检查

- 产品文档：已同步 `docs/product/mvp.md` 的历史入口名称。
- 架构文档：已同步 `docs/architecture/system-overview.html` 的入口名称。
- 决策文档：不新增长期决策。
- 文档入口：实施后运行文档引用检查。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-09-14 00:00 CST | Codex | 创建待确认方案；建议历史入口使用“旧时日记”。 |
| 2026-09-14 13:00 CST | 用户 | 明确批准方案。 |
| 2026-09-14 13:15 CST | Codex | 完成源码、测试和文档同步，状态更新为待验收。 |
| 2026-09-14 13:30 CST | 用户 | 验收通过并明确要求创建提交。 |
| 2026-09-14 13:30 CST | Codex | 已满足提交前验证条件，更新为已完成并准备提交。 |
