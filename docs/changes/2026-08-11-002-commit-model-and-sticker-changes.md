# DD-20260811-002：提交模型与贴纸相关改动

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 待验收 |
| 类型 | 工程管理 / 文档 |
| 创建时间 | 2026-08-11 18:37 CST |
| 最后更新 | 2026-08-11 18:39 CST |
| 当前阶段 | 已完成实现提交、文档回写和验证，等待用户确认提交结果 |
| 源码基线 | Git commit `c7a3660`；分支 `main` |
| 实现提交 | `9833d6750ba448bda616b8fec4f1466e8b6b311d`；本记录所在文档收口提交 |
| 关联任务 | 用户要求提交当前工作区的模型优化与贴纸 bug 相关改动；关联 `DD-20260810-002`、`DD-20260811-001` |

> 用户于 2026-08-11 直接要求提交当前工作区改动，构成对本地提交动作的直接执行授权。本任务不推送远端、不创建 PR，也不把提交动作解释为对模型视觉的验收。

## 1. 给阅读者的结论

当前工作区主体是已批准并实施的桌子、桌垫和本子模型优化，同时包含 DOM 日记打开后隐藏 3D 本子以消除页块与贴纸纸面重影的局部修正、对应测试和事实文档。另有一份贴纸持久化异常的只读诊断记录；该问题已定位但没有业务修复，提交说明不得宣称 `Invalid argument to Table.get()` 已修复。

计划先提交可运行的模型与重影修正，再用文档提交回写真实提交号并纳入本记录和贴纸异常诊断记录。两项提交均保留原任务的待验收或待确认状态。

## 2. 用户需求

- 用户原始要求：提交当前工作区的更改，内容概括为“模型优化”和“修复贴纸 bug”。
- Codex 解释：核对真实差异后创建本地 Git 提交；其中可由源码证明的修复是 3D 本子与 DOM 日记/贴纸纸面的视觉重影。`Table.get()` 贴纸数据异常当前只有诊断记录，不能写成已修复。

## 3. 当前源码事实

- 当前分支为 `main`，`HEAD` 与 `origin/main` 均为 `c7a3660`。
- `DD-20260810-002` 已处于待验收，记录了程序化桌子、桌垫、本子工厂、共享 PBR 材质、对象审查入口、资源释放、测试和双视口浏览器验证。
- `NotebookObject` 使用 `isNotebookModelVisible(notebookPhase)`，只在 `editing` 阶段隐藏 3D 本子；测试覆盖其余转场阶段继续可见。该行为解决 DOM 双页拥有打开页时 3D 页块穿出、干扰纸面与日记贴纸视觉的问题。
- `DD-20260811-001` 只读定位了贴纸关联字段为空导致 Dexie `Table.get()` 报错的路径；当前没有 `sticker-repository.ts` 源码差异，因此不能宣称该持久化问题已修复。
- 工作区还包含模型评审图、规格、PBR 证据、产品说明、架构 HTML、文档入口和旧视觉任务状态回写，均由原任务记录说明。

## 4. 目标与非目标

### 4.1 目标

- 将当前模型实现、视觉重影修正、测试、评审证据和同步文档完整提交。
- 将贴纸持久化异常的诊断记录纳入版本库，但准确保持为诊断结论。
- 回写原任务和本任务的真实提交号，确保文档与 Git 历史一致。

### 4.2 非目标

- 不在本任务新增业务修复、迁移或持久化数据写入。
- 不把 `Table.get()` 诊断冒充为修复。
- 不自行把模型任务标记为已完成。
- 不推送远端、不创建分支或 PR。

## 5. 方案说明

先运行完整项目检查、差异检查和文档引用检查。通过后，把模型源码、重影修正、测试、评审资产以及已同步的产品/架构文档作为一个原子实现提交；随后更新任务记录中的真实提交号，并把本记录和贴纸异常诊断记录作为文档收口提交。

## 6. 预计改动与影响评估

- 第一笔提交覆盖 `src/app/`、`src/scene/`、`src/scene/models/`、模型任务记录、评审资产及相关产品/架构/入口文档。
- 第二笔提交覆盖本记录、贴纸异常诊断记录和实现提交号回写。
- 不修改依赖、IndexedDB schema、repository、领域模型或公共业务接口。

### 6.1 核心数据结构变化

本提交整理任务本身不改变数据结构。被提交的模型实现只新增场景内部 spec 与运行时 `sculptRuntime`；现有领域状态和持久化模型保持不变。

### 6.2 上下游与跨模块影响

模型工厂由 `DeskScene` 和 `NotebookObject` 消费，开发审查入口由 `App` 按查询参数惰性加载。视觉重影修正只改变 `editing` 阶段的 3D 本子可见性，不改变 DOM 日记、贴纸实例或本子状态机协议。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 评审资产遗漏 | 只暂存已跟踪文件 | 模型证据与任务记录失配 | 显式暂存任务目录并检查缓存区文件清单 |
| 提交说明夸大贴纸修复 | 把诊断记录与重影修正混为一谈 | Git 历史与源码事实不一致 | 提交说明只声明可验证的 journal overlap 修正；诊断使用独立文档表述 |
| 提交后文档仍写“尚未创建” | 未回写提交号 | 项目追溯链断裂 | 使用第二笔文档提交收口 |

回退可按两笔提交逆序 revert；不需要数据库降级或用户数据清理。

## 8. 验证与验收

- 自动测试：运行 `npm run check` 中的全量 Vitest。
- 构建与静态检查：运行 ESLint、TypeScript/Vite production build、`git diff --check` 和缓存区差异检查。
- 浏览器验收：本任务不新增业务或样式改动，沿用 `DD-20260810-002` 已完成的桌面与移动端 `ego-browser` 证据。
- 持久化与恢复：本任务不改持久化实现；`Table.get()` 问题仍待后续批准修复。
- 成功标准：两类实际改动和全部必要文档进入本地提交，提交说明准确，工作区与缓存区无遗漏。

## 9. 待确认项与决策

无阻塞待确认项。用户已明确授权本地提交；贴纸持久化异常是否实施容错和迁移仍由 `DD-20260811-001` 后续单独确认。

## 10. 最终批准方案

用户于 2026-08-11 直接授权执行。最终清单：核对当前差异；运行完整验证；创建模型与重影修正实现提交；回写真实提交号；创建文档收口提交；不推送。

## 11. 实施记录

已按计划完成：

- 创建实现提交 `9833d6750ba448bda616b8fec4f1466e8b6b311d`，提交说明为 `feat(scene): refine models and fix journal overlap`。
- 实现提交包含 171 个文件：三个程序化模型工厂、共享 PBR 材质与环境、开发审查入口、模型结构/资源/预算测试、DOM/3D 可见性修正、模型评审证据，以及产品、架构、入口和原视觉任务记录。
- `DD-20260810-002` 已回写真实实现提交号并继续保持“待验收”；本次提交授权没有被解释为用户已接受最终视觉。
- `DD-20260811-001` 作为独立诊断记录纳入文档收口提交，继续保持“待确认”。当前不存在 `sticker-repository.ts` 修改，因此没有把贴纸关联数据异常写成已修复。
- 本记录随文档收口提交进入版本库；两笔提交均为本地提交，没有推送远端或创建 PR。

方案无实质偏差。用户概括中的“贴纸 bug”按真实源码拆分为已修正的日记纸面视觉重影，以及尚待单独批准的贴纸持久化异常诊断。

## 12. 验证结果

- `TMPDIR=/tmp TMP=/tmp TEMP=/tmp npm run check` 通过：ESLint 通过；Vitest 11 个文件、41 个用例全部通过；TypeScript/Vite production build 通过；文档引用检查通过。
- Vite 保留已有的大 chunk 提示，未出现构建错误。
- `git diff --check` 与 `git diff --cached --check` 通过。
- 第一笔提交前核对缓存区为 171 个预期文件，两份 `2026-08-11` 收口记录和 `sticker-repository.ts` 均未混入。
- 本任务没有重复浏览器验证；视觉与交互证据沿用 `DD-20260810-002` 已完成的桌面、移动和 reduced-motion 验收。

## 13. 文档同步检查

- 产品文档：沿用模型任务已完成的当前事实同步。
- 架构文档：沿用模型任务已完成的模型所有权与运行链路同步。
- 决策文档：不新增长期约束。
- 文档入口：沿用模型任务已完成的入口同步；本记录为 Markdown，不新增 HTML 修改。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-08-11 18:37 CST | 用户 | 要求提交当前工作区的模型优化与贴纸 bug 相关改动。 |
| 2026-08-11 18:37 CST | Codex | 完成初步归属核对；确认一项可验证的 DOM/3D 重影修正，以及一项尚未实施的贴纸持久化异常诊断，开始完整验证与提交。 |
| 2026-08-11 18:38-18:39 CST | Codex | 全量检查通过；创建实现提交 `9833d67`，回写真实提交号并准备文档收口提交，任务进入待验收。 |
