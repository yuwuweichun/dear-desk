# DD-20260903-008：Vite 环境拦截时在受限环境外执行

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 待验收 |
| 类型 | 文档 |
| 创建时间 | 2026-09-03 23:45 CST |
| 最后更新 | 2026-09-03 23:50 CST |
| 当前阶段 | 规则补充完成，等待验收 |
| 源码基线 | 当前工作树（含用户已有未提交改动） |
| 实现提交 | 尚未创建 |
| 关联任务 | 明确 Windows 沙箱拦截 Vite spawn EPERM 时的后续执行方式 |

## 1. 给阅读者的结论

补充项目协作规则：如果 Vite 在加载配置或启动子进程时被 Windows 受限环境以 `spawn EPERM` 拦截，且不是代码断言或测试逻辑失败，应直接在受限环境外重试同一命令。该规则只处理已确认的执行环境限制，不改变测试失败的判断标准。

## 2. 用户需求

- 用户原始要求：更新 `AGENTS.md`；以后遇到类似任务，直接在受限环境外运行。
- Codex 解释：为 Vite/Windows `spawn EPERM` 环境拦截增加明确的升级执行规则。

## 3. 当前源码事实

- 当前请求针对项目协作规则，不修改业务源码、依赖、配置或持久化数据。
- 用户已说明此前现象是 Windows 受限环境拦截 Vite 配置加载时的 `spawn EPERM`，不是代码断言失败。

## 4. 目标与非目标

### 4.1 目标

- 在 `AGENTS.md` 中记录上述环境限制的处理方式。

### 4.2 非目标

- 不修改 Vite 配置、测试代码或 npm 脚本。
- 不把普通测试失败、构建错误或未知权限错误自动归因于环境限制。

## 5. 方案说明

在工程与验证约束中增加一条规则，限定触发条件为 Windows 受限环境中的 Vite `spawn EPERM` 拦截，并要求使用同一命令、同一工作目录在受限环境外重试。

## 6. 预计改动与影响评估

只修改根目录 `AGENTS.md`，不改变运行时代码或产品行为。新增规则会影响后续测试与构建命令的执行位置判断。

### 6.1 核心数据结构变化

不适用；没有数据结构、接口或持久化变化。

### 6.2 上下游与跨模块影响

不适用；仅影响 Codex 执行项目命令时的环境选择。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 误把真实失败当作环境失败 | 错误不是明确的 `spawn EPERM` | 可能掩盖代码问题 | 仅对已确认的 Vite/Windows 受限环境拦截重试；其他错误按原规则处理 |

## 8. 验证与验收

- 自动测试：不适用，未修改源码。
- 构建与静态检查：不运行 Vite；本次规则本身通过差异与文档引用检查验证。
- 浏览器验收：不适用。
- 持久化与恢复：不适用。
- 成功标准：`AGENTS.md` 明确包含触发条件、执行位置和边界。

## 9. 待确认项与决策

无。用户已明确指定处理方式。

## 10. 最终批准方案

用户请求本身作为本次文档规则更新授权；执行清单为：创建本记录、更新 `AGENTS.md`、检查差异与文档引用。

## 11. 实施记录

已完成。仅修改根目录 `AGENTS.md`，在工程与验证约束中加入 Windows 受限环境下 Vite `spawn EPERM` 的受限环境外重试规则。

## 12. 验证结果

已完成：检查 `git diff --check`；检查变更记录与 `AGENTS.md` 的路径引用。未运行 Vite、测试或构建，因为本次只修改协作规则文档。

## 13. 文档同步检查

- 产品文档：不适用。
- 架构文档：不适用。
- 决策文档：不适用。
- 文档入口：不适用。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-09-03 23:45 CST | Codex | 创建并实施文档规则更新。 |
