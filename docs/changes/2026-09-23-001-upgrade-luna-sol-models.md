# DD-20260923-001：升级 Luna 与 Sol 项目模型版本

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已完成 |
| 类型 | 配置 / 文档 |
| 创建时间 | 2026-09-23 09:05 CST |
| 最后更新 | 2026-09-23 09:10 CST |
| 当前阶段 | 已实施并提交 |
| 源码基线 | af78792ff9a9f6d9e695769cd5af39fa0a886376 |
| 实现提交 | 本次提交 |
| 关联任务 | 将项目级 Luna/Sol Codex 模型从 5.6 升级到 6 |

> 用户明确要求直接更新项目级 Codex 配置，构成本任务的实施授权；不修改业务源码，不创建提交、推送或 PR。

## 1. 给阅读者的结论

项目级 Codex 工作流将继续保持“Luna 执行、Sol 推理”的职责划分，但使用 `gpt-6-luna` 作为主任务与默认子代理模型，使用 `gpt-6-sol` 作为只读 Sol 推理代理模型。

## 2. 用户需求

- 原始要求：将原先 Luna -> Sol 的项目级 Codex 配置从 5.6 改为 6。
- Codex 解释：同步当前配置、协作规则和长期决策文档；保留历史记录中的 5.6 事实。

## 3. 当前源码事实

- `.codex/config.toml` 当前主任务与默认子代理使用 `gpt-5.6-luna`。
- `.codex/agents/sol-reasoner.toml` 当前使用 `gpt-5.6-sol`。
- `AGENTS.md` 与 `docs/decisions/2026-08-31-001-luna-execution-sol-reasoning.md` 当前仍记录 5.6 模型。
- 相关历史变更记录描述的是 2026-08-31 当时的配置事实，不应改写为当前版本。

## 4. 目标与非目标

### 4.1 目标

- 将当前主任务和默认子代理切换为 `gpt-6-luna`。
- 将当前 `sol_reasoner` 切换为 `gpt-6-sol`。
- 同步当前协作规则与长期决策文档。

### 4.2 非目标

- 不修改业务源码、依赖、产品行为或持久化数据。
- 不改变 Luna/Sol 的职责边界、推理强度、沙箱权限或升级条件。
- 不改写历史变更记录中的旧模型版本。

## 5. 方案说明

在现有文件中做最小替换：只更新当前模型标识及其描述文字，保留 `medium`、`high`、只读沙箱和双模型工作流规则不变。

## 6. 预计改动与影响评估

- `.codex/config.toml`：更新主任务和默认子代理模型。
- `.codex/agents/sol-reasoner.toml`：更新 Sol 模型和描述。
- `AGENTS.md`：更新当前协作规则中的 Luna 模型名。
- `docs/decisions/2026-08-31-001-luna-execution-sol-reasoning.md`：更新当前长期决策中的模型名。
- `.codex` 配置仅对新加载项目任务生效，不热切换已运行任务。

### 6.1 核心数据结构变化

无数据结构、接口、持久化模型或运行时状态变化；仅替换配置字符串。

### 6.2 上下游与跨模块影响

仅影响 Codex 项目任务启动时的模型路由；不影响产品运行时、构建产物和浏览器数据。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 新模型标识不可解析 | Codex 运行环境不支持该标识 | 新任务无法按预期启动 | 使用 Git 差异回退为 5.6 配置 |

## 8. 验证与验收

- 自动测试：使用 Python 标准库 `tomllib` 解析两个 TOML 文件。
- 构建与静态检查：不需要，未修改应用源码。
- 浏览器验收：不适用。
- 持久化与恢复：不适用。
- 成功标准：当前配置与当前文档统一使用 `gpt-6-luna` / `gpt-6-sol`，历史记录保留旧版本事实，TOML 可解析。

## 9. 待确认项与决策

无。用户要求已明确，且授权直接执行。

## 10. 最终批准方案

2026-09-23，用户明确要求将项目级 Luna/Sol 配置从 5.6 升级到 6，按最小范围直接执行。

## 11. 实施记录

- 更新 `.codex/config.toml`：主任务与默认子代理改为 `gpt-6-luna`。
- 更新 `.codex/agents/sol-reasoner.toml`：只读 Sol 代理改为 `gpt-6-sol`，同步描述文字。
- 更新 `AGENTS.md` 与 `docs/decisions/2026-08-31-001-luna-execution-sol-reasoning.md` 的当前模型事实。
- 未改写 2026-08-31 历史变更记录中的 `gpt-5.6`。

## 12. 验证结果

- Node.js 字符串断言通过：两个当前 TOML 文件均包含目标模型声明，分别为 `gpt-6-luna` 与 `gpt-6-sol`。
- `git diff --check` 通过。
- Python `tomllib` 未执行：当前环境的 `python.exe` 是 Microsoft Store 占位入口，未提供可运行解释器；配置内容为现有 TOML 结构的字符串替换。
- 用户明确要求创建提交，提交前验收条件满足。

## 13. 文档同步检查

- 产品文档：不涉及产品行为。
- 架构文档：不涉及产品架构。
- 决策文档：同步更新双模型长期决策。
- 文档入口：不涉及。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-09-23 09:05 CST | Codex | 创建记录并确认最小修改范围。 |
