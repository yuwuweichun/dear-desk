# DD-20260903-006：明确中文 Git 提交描述规则

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已完成 |
| 类型 | 文档 |
| 创建时间 | 2026-09-03 22:00 CST |
| 最后更新 | 2026-09-03 22:05 CST |
| 当前阶段 | 已完成 |
| 源码基线 | `334b232d484935cfb70ca2773a3b47bae249e3d8` |
| 实现提交 | 本次提交（创建后回填） |
| 关联任务 | 用户要求项目级 Git commit message 的描述部分采用中文 |

## 1. 给阅读者的结论

项目协作规则现已明确：提交消息继续使用 Conventional Commits 的英文 `type(scope)` 前缀，但冒号后的描述必须使用中文。

## 2. 用户需求

用户要求项目级规则采用“英文 type/scope + 中文描述”，例如 `feat(scene): 调整前置镜头覆盖范围`。

## 3. 当前源码事实

`AGENTS.md` 原只规定提交消息使用 `type(scope): description`，没有规定描述语言。

## 4. 目标与非目标

### 4.1 目标

- 在项目级协作规则中明确提交描述使用中文。

### 4.2 非目标

- 不修改历史提交、分支、业务源码、依赖或 Git 配置。

## 5. 方案说明

直接补充 `AGENTS.md` 的 Git 约束，并给出符合项目习惯的示例。

## 6. 预计改动与影响评估

- 修改 `AGENTS.md` 的提交消息规则。
- 新增本任务记录，记录实际变更与验收状态。

### 6.1 核心数据结构变化

无数据结构、接口或持久化变化。

### 6.2 上下游与跨模块影响

仅影响后续人工和 Codex 创建 Git 提交时的消息格式；不影响运行时代码。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 规则理解不一致 | 描述中混入英文 | 提交消息不符合项目约定 | 以后按示例检查；必要时回退本次文档修改 |

## 8. 验证与验收

- 自动测试：不适用，纯文档规则变更。
- 构建与静态检查：不适用。
- 浏览器验收：不适用。
- 持久化与恢复：不适用。
- 成功标准：`AGENTS.md` 明确要求英文 `type/scope` 与中文描述组合。

## 9. 待确认项与决策

无。用户要求已足够明确。

## 10. 最终批准方案

用户消息直接确认项目级规则；按“英文 `type(scope)` 前缀 + 中文描述”写入 `AGENTS.md`。用户随后明确要求创建提交，构成最终结果预先验收授权。

## 11. 实施记录

已修改 `AGENTS.md`，将规则更新为 `type(scope): 中文描述`，并加入示例。

## 12. 验证结果

已通过文本检查确认规则存在于 `AGENTS.md`，并通过 `git diff --check`。未运行测试或构建，原因是本次只修改协作文档。

## 13. 文档同步检查

- 产品文档：不适用，无产品行为变化。
- 架构文档：不适用，无架构变化。
- 决策文档：不适用，属于项目协作规则补充。
- 文档入口：不适用。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-09-03 22:00 CST | Codex | 实施项目级中文提交描述规则。 |
| 2026-09-03 22:05 CST | 用户 / Codex | 用户明确要求提交本次更改；验证通过后将任务记录纳入同一提交并标记为已完成。 |
