# DD-20260821-003：OFL 字体事实与任务状态收尾

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已替代 |
| 类型 | 文档 / 状态收尾 / 调研 |
| 创建时间 | 2026-08-21 23:29 CST |
| 最后更新 | 2026-08-21 23:29 CST |
| 当前阶段 | 历史字体事实已被 DD-20260821-004 替代 |
| 源码基线 | `d3d4594`；工作区初始干净 |
| 实现提交 | 本次提交（由 Git 记录） |
| 关联任务 | 用户要求汇报当前 OFL 字体、取消暖色主光实验，并收尾已验收/过期任务 |

> 用户已明确给出本次状态决策，授权直接执行文档收尾。未修改业务源码、字体资源、依赖、配置或持久化数据。

## 1. 给阅读者的结论

本文记录的字体事实已由 `DD-20260821-004` 的实施结果替代。当前生产日记字体为纸页宋体回退、玄冬楷书、随峰体和志莽行书；云峰晶晶体已删除。全局 UI 仍由 `animal-island-ui` 提供 Nunito/Noto Sans SC，依赖整体许可证为 CC BY-NC-4.0，不能把它们汇报为 OFL 字体。

本次将用户确认通过的自定义铭牌、铭牌文字朝向和 `Table.get` 修复记录标记为已完成；铭牌凹刻纹理与颜色对比继续待验收；暖色主光实验取消；旧字体替换提案和过时字体盘点标记为已替代。

## 2. 用户需求

- 汇报当前使用的字体及其许可证边界，生产字体优先采用 OFL 资源。
- 取消暖色主方向光对比实验。
- 将用户明确验收通过的任务标记为已完成。
- 保留铭牌纹理和颜色为待验收。
- 将项目字体盘点标记为过期/作废。

## 3. 当前源码事实

- `src/domain/journal-font.ts` 定义 `paper`、`jingjing`、`xuandong`、`suifeng` 四个稳定 ID。
- `src/features/journal/JournalPanel.tsx` 在生产环境过滤掉 `jingjing`，因此生产菜单为纸页宋体、玄冬楷书、随峰体；开发环境额外显示云峰晶晶体。
- `src/styles.css` 使用 `Georgia, "Songti SC", serif` 作为纸页宋体回退，`public/fonts/Xuandong-Kaishu.ttf` 和 `public/fonts/ThePeakFontPlus-Regular.ttf` 作为生产本地资源。
- `public/fonts/ThePeakFontPlus-OFL.txt` 保存 SIL OFL 1.1 原文；`ThePeakFontPlus-SOURCE.txt` 记录官方来源、V1.002 和哈希。玄冬楷书的 OFL 来源事实记录在 `DD-20260819-002` 与 `dev-assets/fonts/README.md`。
- `node_modules/animal-island-ui/LICENSE` 为 CC BY-NC-4.0；它提供全局 UI 的 Nunito/Noto Sans SC 资源，不能归入 OFL 日记字体清单。
- `src/scene/DeskScene.tsx` 的暖色主方向光仍为 `intensity={1.68}`；本次只取消实验，不改变现有运行行为。

## 4. 目标与非目标

### 4.1 目标

- 形成当前字体与许可证的可审计报告。
- 回写相关变更记录的正式状态和审阅记录。

### 4.2 非目标

- 不替换字体、不删除开发字体、不改变字体菜单或日记数据。
- 不关闭或调整当前生产暖色主光，只取消未完成的对比实验。
- 不修改铭牌纹理、铭牌颜色或其他视觉实现。

## 5. 方案说明

以当前源码、字体资源许可证文件和依赖许可证为准，区分生产日记字体、开发专用字体与全局 UI 字体。状态收尾只更新变更记录中的状态、阶段和审阅证据；过期提案使用“已替代”，取消的实验使用“已取消”。

## 6. 预计改动与影响评估

- 更新相关 `docs/changes/` 记录的状态、当前阶段、提交事实和审阅记录。
- 将 `DD-20260820-001` 替代为当前 OFL 字体方案，将 `DD-20260820-002` 标记为历史盘点。
- 产品文档补充字体许可证边界；不改变运行时行为。

### 6.1 核心数据结构变化

无。字体 ID、CSS、IndexedDB、日记正文和 Three.js 状态均不变。

### 6.2 上下游与跨模块影响

仅影响文档状态和事实说明。代码、构建产物、持久化数据和用户交互不变。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 将依赖 UI 字体误报为 OFL | 忽略 `animal-island-ui` 整体许可证 | 许可证事实不准确 | 单独列出 CC BY-NC-4.0 依赖边界 |
| 过期盘点被继续当作当前事实 | 只修改状态不注明时间截面 | 用户误读字体集合 | 新记录给出当前源码和资源清单 |
| 误把取消实验理解为关闭主光 | 将实验状态与生产光照混淆 | 运行视觉行为意外改变 | 明确 `DeskScene` 保持 `1.68`，仅取消实验 |

## 8. 验证与验收

- 自动检查：字体源码、许可证、状态字段和 Git 差异核对。
- 构建与静态检查：运行文档引用检查、`git diff --check`；不需要重新构建业务源码。
- 浏览器验收：本次无用户可见运行时改动，不执行浏览器视觉验收。
- 成功标准：生产字体报告与源码一致；指定任务状态与用户决策一致；暖色主光实验为已取消。

## 9. 待确认项与决策

无新的范围或架构待确认项。全局 UI 字体是否未来迁移到 OFL 不在本次范围内。

## 10. 最终批准方案

用户于 2026-08-21 明确给出验收通过、待验收、取消和过期/作废决策，按直接执行授权完成本次文档收尾。

## 11. 实施记录

- `DD-20260806-005`、`DD-20260819-003`、`DD-20260811-001` 更新为 `已完成`。
- `DD-20260819-008` 更新为 `已取消`。
- `DD-20260820-001`、`DD-20260820-002` 更新为 `已替代`。
- `DD-20260819-004`、`DD-20260819-005` 保持 `待验收`。
- `docs/product/mvp.md` 补充当前日记字体与许可证边界说明。

## 12. 验证结果

- `node scripts/check-doc-references.mjs`：通过。
- `git diff --check`：通过。
- 生产字体事实：纸页宋体回退、玄冬楷书、随峰体；开发专用云峰晶晶体不进入生产构建；全局 UI 字体来自 CC BY-NC-4.0 依赖。
- 未执行浏览器验证，原因是本次没有运行时改动。

## 13. 文档同步检查

- 产品文档：更新日记字体与许可证边界。
- 架构文档：当前 HTML 架构页已有随峰体事实；本次不绕过 `$bun-html-docs` 修改 HTML。
- 决策文档：无新增长期决策。
- 文档入口：`docs/index.html` 的状态摘要仍需在 `$bun-html-docs` 可用时同步；本次不直接修改 HTML。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-08-21 23:29 CST | 用户 / Codex | 用户明确给出当前字体、取消暖色主光、验收通过、待验收及过期任务决策；创建本次状态收尾记录并直接执行。 |
| 2026-08-21 23:29 CST | Codex | 文档引用检查与 `git diff --check` 通过；未执行浏览器视觉验收，因当前环境没有可用的 `ego-browser` 连接。 |
| 2026-08-22 00:05 CST | 用户 / Codex | `DD-20260821-004` 获批准并完成实施；本文的历史字体事实由新记录替代，任务状态更新为已替代。 |
