# DD-20260914-003：全库 Ponytail 代码简化审计

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已完成 |
| 类型 | 调研 |
| 创建时间 | 2026-09-14 00:00 CST |
| 最后更新 | 2026-09-14 18:10 CST |
| 当前阶段 | 已完成 |
| 源码基线 | 55203bb feat(hud): 调整桌面操作入口文案与顺序 |
| 实现提交 | 本次提交 |
| 关联任务 | 用户请求全库冗余与代码简化审计，并询问 Ponytail 流程 |

## 1. 给阅读者的结论

本记录用于记录一次只读的全库过度工程审计。审计输出按“可删除、可用原生能力替代、可收缩”排序，不直接修改业务源码。

## 2. 用户需求

用户希望了解当前各功能和整个代码库是否存在可优化或冗余之处，以及 Ponytail 是否能用于现有代码简化。解释为：扫描现有实现，给出有证据的最小化建议与可执行流程。

## 3. 当前源码事实

仓库为 Vite + React + TypeScript + Three.js/R3F 应用，包含业务源码、测试、产品/架构/决策文档、演示文档和第三方资源。当前基线为 `55203bb`，审计开始时 Git 工作区无未提交改动。源码最大文件包括 `src/state/app-store.ts`（约 36 KB）、`src/scene/DeskScene.tsx`（约 34 KB）和多个模型工厂；它们承载真实状态机、场景投影和资源生命周期，不因体积本身视为冗余。

高置信度残留：`src/scene/scene-visuals.ts` 仅被自身历史文档文字提及，当前运行时代码使用 `src/scene/models/material-library.ts`；`src/scene/RoundedBox.tsx` 仅被历史文档文字提及，当前模型路径没有导入；`public/assets/wallpapers/candy-cloud-cube-net.png` 仅被历史任务记录提及，当前运行时使用 `candy-cloud-panorama.png`，且 `DD-20260913-011` 已明确其为旧 atlas。其余观察到的 repository interface、model factory runtime 和 review scene 都有测试、依赖注入或开发审查调用链，不属于可直接删除项。

## 4. 目标与非目标

### 4.1 目标

- 识别全库范围内的过度工程、死代码、重复封装和可由原生能力替代的实现。
- 评估 Ponytail 是否适合后续实施简化。

### 4.2 非目标

- 不修复功能 bug、安全问题或性能问题。
- 不修改业务源码、依赖、配置或持久化数据。

## 5. 方案说明

读取产品与架构事实、检查 Git 和依赖，追踪源码导入/调用关系，结合静态检查与测试结果形成按优先级排序的一行式审计清单。只有用户批准具体清单后，才另行创建实施任务或进入重构。

## 6. 预计改动与影响评估

本次删除两个已无运行时引用的旧源码文件和一个已被 panorama 替代的旧墙纸 atlas；业务行为、依赖、数据结构不变。

### 6.1 核心数据结构变化

无。

### 6.2 上下游与跨模块影响

无运行时影响。审计会覆盖 `src/`、工程配置、测试、文档和资源目录。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 静态未使用判断误报 | 动态导入、资源引用或测试外部使用 | 错误建议删除 | 只将有调用/导入证据的项列为高置信度；疑似项标为待确认 |

## 8. 验证与验收

- 自动测试：`npm run test -- --reporter=dot`（受限环境外）通过，32 个文件、138 个测试通过；测试 stderr 存在 React `act(...)` 警告，但无失败。
- 构建与静态检查：`npm run lint` 通过；`npm run build` 通过；文档引用检查通过。受限环境首次运行 Vitest 因 Vite `spawn EPERM` 失败，按项目规则在同一目录外部重跑后通过。
- 浏览器验收：非目标，未执行。
- 持久化与恢复：非目标，未执行。
- 成功标准：输出带路径和替代方案的排序清单，并说明可否采用 Ponytail 流程。已满足。

## 9. 待确认项与决策

审计报告后由用户决定是否批准某一项或一组简化进入独立实施任务。

## 10. 最终批准方案

用户于 2026-09-14 明确回复“批准”，并于完成验证后明确“验收通过，commit”。最终执行清单：删除 `src/scene/scene-visuals.ts`、删除 `src/scene/RoundedBox.tsx`、删除 `public/assets/wallpapers/candy-cloud-cube-net.png`；运行 lint、测试、build、文档引用检查；回写实际差异。

## 11. 实施记录

已完成：删除 `src/scene/scene-visuals.ts`、`src/scene/RoundedBox.tsx` 和 `public/assets/wallpapers/candy-cloud-cube-net.png`。未修改业务逻辑、依赖、配置、测试或持久化数据。实际差异与批准清单一致。

## 12. 验证结果

删除后验证通过：`npm run check` 成功，包含 lint、32 个测试文件/138 项测试、TypeScript/Vite build 和文档引用检查。生产构建仍有 Vite 大 chunk 提示（约 1.34 MB JS、23 MB WASM），属于性能/发布优化，不在本次 Ponytail 范围内。

## 13. 文档同步检查

- 产品文档：本次不改变产品行为。
- 架构文档：本次不改变架构事实。
- 决策文档：本次不新增长期决策。
- 文档入口：本次不修改入口。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-09-14 00:00 CST | Codex | 创建只读审计记录。 |
| 2026-09-14 18:10 CST | Codex | 完成全库 Ponytail 审计与基线验证，未修改业务源码。 |
| 2026-09-14 18:10 CST | Codex | 用户批准清单；删除 2 个旧源码文件和 1 个旧墙纸资源，完整检查通过。 |
| 2026-09-14 18:15 CST | Codex | 用户验收通过并授权创建提交；任务记录标为已完成。 |
