# DD-20260913-012：直接在受限环境外重跑 Vite 验证

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已完成 |
| 类型 | 文档 |
| 创建时间 | 2026-09-13 00:00 CST |
| 最后更新 | 2026-09-13 00:00 CST |
| 当前阶段 | 已验证并提交 |
| 源码基线 | 当前工作区；存在上一任务未提交改动 |
| 实现提交 | 本次提交：`docs(workflow): 更新 Vite 验证规则` |
| 关联任务 | 将已知 Vite `spawn EPERM` 受限环境限制写入项目 `AGENTS.md`，避免每次验证重复失败尝试 |

> 用户已明确要求将该规则作为项目协作规定，视为无需额外方案审批的直接执行授权。

## 1. 给阅读者的结论

项目后续遇到明确的 Vite `spawn EPERM` 受限环境启动限制时，将直接使用相同命令和工作目录在受限环境外重跑，不再先重复一次已知会失败的受限环境尝试。其他测试、lint 或构建错误仍按真实失败处理。

## 2. 用户需求

用户要求：验证经常被同一个 Vite `spawn EPERM` 启动限制拦截时，后续需要验证应直接在受限环境外重跑目标测试，并将此规则写入项目 `AGENTS.md`。

Codex 解释：规则只覆盖错误信息明确属于受限环境 Vite 启动/子进程限制的情况，不掩盖代码断言失败或其他环境问题。

## 3. 当前源码事实

- `AGENTS.md` 的“工程与验证约束”已有 Vite `spawn EPERM` 例外，但当前措辞只说明可在受限环境外重试，未明确后续应跳过重复的受限环境尝试。
- 当前工作区存在上一任务的未提交源码和文档改动，本任务不修改这些文件。

## 4. 目标与非目标

### 4.1 目标

- 在 `AGENTS.md` 中明确：确认是受限环境 Vite `spawn EPERM` 后，直接用相同命令和工作目录在受限环境外重跑。
- 保留其他验证错误的正常失败语义。

### 4.2 非目标

- 不修改 Vite 配置、测试脚本、依赖或业务源码。
- 不改变受限环境外命令的工作目录、参数或验证范围。

## 5. 方案说明

只收紧现有 Vite 例外条款，在同一条规则中定义识别条件、重跑方式和边界，避免新增脚本、环境变量或自动化包装层。

## 6. 预计改动与影响评估

预计只修改 `AGENTS.md`，将现有 Vite `spawn EPERM` 条款改为“确认后直接外部重跑”。变更影响后续 Codex 验证流程，不影响应用运行时。

### 6.1 核心数据结构变化

不涉及数据结构、接口、持久化模型或配置格式。

### 6.2 上下游与跨模块影响

不涉及源码调用链；只影响测试、lint、build 等验证命令在特定 Windows 受限环境错误下的执行路径。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 将非 Vite 错误误判为环境限制 | 错误并非明确 `spawn EPERM` | 可能跳过必要排查 | 规则要求错误明确属于受限环境 Vite 启动/子进程限制；其他错误不适用 |

## 8. 验证与验收

- 自动测试：不适用，纯协作文档规则变更。
- 构建与静态检查：不运行，未修改源码、依赖或构建配置。
- 浏览器验收：不适用。
- 持久化与恢复：不适用。
- 成功标准：`AGENTS.md` 明确规定确认 `spawn EPERM` 后直接外部重跑，并保留其他错误的失败语义。

## 9. 待确认项与决策

无。用户已明确要求直接写入项目规则。

## 10. 最终批准方案

用户请求即为直接执行授权。执行清单：仅修改 `AGENTS.md` 中现有 Vite `spawn EPERM` 验证条款，并保留本记录。

## 11. 实施记录

已实施：

- 更新 `AGENTS.md` 的工程与验证约束，规定明确识别到受限环境 Vite `spawn EPERM` 后，直接以相同命令和工作目录在受限环境外重跑。
- 明确普通测试、lint、build 错误仍不得按该例外处理。

方案偏差：无。

## 12. 验证结果

- 已通过文本检查确认 `AGENTS.md` 包含直接外部重跑规则及错误边界。
- `node scripts/check-doc-references.mjs`：通过。
- `git diff --check`：通过。
- 未运行项目测试、lint 或 build，因为本次不涉及源码或工程配置。

## 13. 文档同步检查

- 产品文档：不涉及。
- 架构文档：不涉及。
- 决策文档：不新增，规则属于项目协作说明。
- 文档入口：不涉及。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-09-13 00:00 CST | Codex | 根据用户直接执行要求创建并实施文档规则变更。 |
| 2026-09-13 00:00 CST | Codex | 用户要求提交本次项目规则更新；完成文档检查后纳入本次提交。 |
