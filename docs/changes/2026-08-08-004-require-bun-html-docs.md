# DD-20260808-004：HTML 文档强制使用 bun-html-docs

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已完成 |
| 类型 | 文档 / 项目协作规则调整 |
| 创建时间 | 2026-08-08 CST |
| 最后更新 | 2026-08-09 19:30 CST |
| 当前阶段 | 开发者（用户）已验收，任务完成 |
| 源码基线 | `fe2fc89eaac75c8aa5843c27e21b098e449d45f2` |
| 实现提交 | 未创建；用户未要求提交 |
| 关联任务 | 将 HTML 文档规则改为强制使用 `$bun-html-docs`，并规定技能不可用时暂停 |

> 用户已明确给出替换文本并要求继续执行，构成直接执行授权；并于 2026-08-09 确认当前工作区未验收成果已完成验收。

## 1. 给阅读者的结论

项目根目录 `AGENTS.md` 的“HTML 文档”章节已经包含用户要求的 `$bun-html-docs` 强制规则：创建或实质修改 HTML 时必须使用该技能，技能不可用或无法读取时必须暂停。当前规则还明确了 `ego-browser` 可用与不可用时各自的验证处理方式。

本轮恢复工作区后确认上述内容已经位于当前 Git 基线，因此没有再次改写 `AGENTS.md`；实际变更仅为修正本记录中已经过时的“尚未实施”描述。

## 2. 用户需求

- 用户原始要求：用指定文本替换 `AGENTS.md` 的“HTML 文档”章节。
- 用户补充要求：`ego-browser` 不可用时允许跳过视觉验证，但必须记录原因、未覆盖范围并要求手动验收。
- 用户在前一轮路径访问失败后要求继续重试。
- Codex 的解释：以当前磁盘和 Git 基线为准核对规则是否已经落地；若已完全落地，不制造无意义差异，只回写任务记录。

## 3. 当前源码事实

- `AGENTS.md` 的“HTML 文档”章节已精确包含 `$bun-html-docs` 强制使用规则。
- 同一章节已规定技能不可用或无法读取 `SKILL.md` 时必须暂停，不得静默换用其他生成方式。
- 同一章节已规定 `ego-browser` 可用时统一使用；不可用时记录原因、未覆盖范围并提示手动验收。
- `git show HEAD:AGENTS.md` 显示这些规则已经存在于基线提交 `fe2fc89eaac75c8aa5843c27e21b098e449d45f2`，`git diff -- AGENTS.md` 为空。
- 当前会话的可用技能清单没有列出 `bun-html-docs`。这是会话能力事实，不影响本次 Markdown 规则核对；以后真正创建或实质修改 HTML 时，必须按规则暂停并提示修复。
- 仓库存在多项与本任务无关的已修改和未跟踪文件，本任务未触碰它们。

## 4. 目标与非目标

### 4.1 目标

- 确认项目规则已强制 HTML 文档任务使用 `$bun-html-docs`。
- 确认技能缺失、`SKILL.md` 无法读取以及 `ego-browser` 不可用时的行为均有明确规定。
- 让任务记录与当前真实文件状态一致。

### 4.2 非目标

- 不创建或修改任何 HTML 文档。
- 不安装、更新或修改 `$bun-html-docs` 技能。
- 不修改业务源码、依赖、工程配置或持久化数据。
- 不处理或回退工作区中其他任务的未提交改动。

## 5. 方案说明

后续 HTML 文档任务进入项目后，先读取项目 `AGENTS.md`，再按技能触发规则读取并执行 `$bun-html-docs` 的完整 `SKILL.md`。技能不可用时流程停止并提示修复；技能可用后才执行 HTML 生成、引用检查和适用的浏览器验证。

本轮先比较磁盘文件、Git 基线和任务记录。由于规则已在基线中落地，保留 `AGENTS.md` 不动，只修正历史记录，避免把无差异操作写成新的源码实施。

## 6. 预计改动与影响评估

- 原预计改动：修改 `AGENTS.md` 的“HTML 文档”章节。
- 实际核对结果：目标章节已存在于 Git 基线，无需产生新的 `AGENTS.md` 差异。
- 实际文档改动：更新 `docs/changes/2026-08-08-004-require-bun-html-docs.md`，补齐模板结构、当前事实、实施与验证结果。

### 6.1 核心数据结构变化

无。判断依据是本任务只涉及 Markdown 协作规则，不触及类型、接口、持久化模型、事件或运行时状态。

### 6.2 上下游与跨模块影响

- 上游：所有创建或实质修改 HTML 文档的后续任务。
- 下游：技能选择、HTML 生成流程、引用检查和视觉验收记录。
- 产品运行时、Three.js 场景、DOM 交互、持久化和测试均不受影响。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 技能未注入当前会话 | 后续任务需要修改 HTML，但技能列表没有 `$bun-html-docs` | HTML 工作必须暂停 | 按 `AGENTS.md` 提示修复或刷新技能，不使用其他方式绕过 |
| 记录与基线混淆 | 把已经存在于 `HEAD` 的规则描述成本轮新写入 | 产生错误实施历史 | 明确记录 `git show` 与 `git diff` 证据，不声明本轮修改了 `AGENTS.md` |
| 污染其他任务改动 | 工作区同时存在其他未提交文件 | 误改或误回退用户内容 | 本任务只触碰当前任务记录，不处理其他文件 |

若用户决定撤销该规则，需要单独批准后修改 `AGENTS.md`，并将本记录标记为“已取消”或“已替代”。本任务没有运行时数据需要回退。

## 8. 验证与验收

- 自动测试：不适用；无运行时代码变化。
- 构建与静态检查：不适用；无源码、依赖或工程配置变化。
- 浏览器验收：不适用；未创建或修改 HTML，也没有用户界面变化。
- 持久化与恢复：不适用；无持久化变化。
- 成功标准：目标规则存在于 `AGENTS.md`；本记录准确反映基线、实际修改、未覆盖项与验收状态。

## 9. 待确认项与决策

无。用户已明确给出目标规则并要求继续。

## 10. 最终批准方案

- 直接执行授权：用户给出完整替换内容，随后明确要求“继续”。
- 最终执行清单：核对 `AGENTS.md` 当前内容和 Git 基线；仅在内容缺失时应用补丁；回写同一任务记录；执行 Markdown 引用和差异检查。
- 范围保持为项目协作规则与任务记录，不触及业务实现。

## 11. 实施记录

- 工作区重新挂载后，成功读取 `/home/song/dear-desk/AGENTS.md`。
- 目标“HTML 文档”章节已经存在于当前磁盘文件和 Git 基线，未重复修改该文件。
- 将本记录从旧的“实施中 / 待实施后回写”内容更新为完整模板结构，并把状态置为“待验收”。
- 方案偏差：原计划产生 `AGENTS.md` 修改；实际发现规则已在基线中落地，因此没有制造空洞或重复差异。

## 12. 验证结果

- `git show HEAD:AGENTS.md`：通过，基线包含 `$bun-html-docs`、技能失败暂停和 `ego-browser` 条件验证规则。
- `git diff -- AGENTS.md`：通过，无本轮差异。
- `rg` 精确规则检查：通过；强制技能名称、技能失败暂停规则和 `ego-browser` 条件验证规则均已匹配。
- Markdown 路径引用检查：通过；本记录引用的两个仓库内文档路径均存在。
- `git diff --check`：通过；未发现空白错误。
- HTML 视觉验证：未执行且不适用，因为本任务没有创建或实质修改 HTML。

## 13. 文档同步检查

- 产品文档：无需修改；产品范围和用户行为未变化。
- 架构文档：无需修改；系统结构、所有权、数据流和公共接口未变化。
- 决策文档：`docs/decisions/2026-08-06-001-documentation-as-review-interface.md` 仍然成立；本规则是其 HTML 流程的执行细化，无需替代原决策。
- 文档入口：无需修改；本任务记录沿用 `docs/changes/` 的既有入口方式，且没有新增 HTML 页面。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-08-08 CST | 用户 / Codex | 用户要求 HTML 文档强制使用 `$bun-html-docs`，Codex 创建任务记录。 |
| 2026-08-08 CST | Codex | 首次尝试因工作区路径未挂载而无法读取 `AGENTS.md`。 |
| 2026-08-09 19:24 CST | 用户 / Codex | 用户要求继续；工作区恢复后确认目标规则已在 Git 基线，回写真实状态，纠正重复编号并进入待验收。 |
| 2026-08-09 19:30 CST | 开发者（用户） | 确认当前工作区未验收成果已验收，同意将本任务标记为已完成并按任务提交记录。 |
