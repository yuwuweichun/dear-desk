# DD-20260903-004：调整浏览器验收默认规则

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已完成 |
| 类型 | 文档 |
| 创建时间 | 2026-09-03 18:45 CST |
| 最后更新 | 2026-09-03 18:48 CST |
| 当前阶段 | 已实施并随本次主题提交完成 |
| 源码基线 | 452df03 |
| 实现提交 | 随本次主题提交创建 |
| 关联任务 | 默认不使用浏览器验收，仅在任务明确说明需要时使用 |

> 用户已直接要求调整项目协作规则，本次按直接执行授权处理。

## 1. 给阅读者的结论

项目协作规则将浏览器验收从默认步骤调整为按需步骤：只有任务或用户明确要求浏览器验收时，才启动浏览器验收及其对应降级流程。

## 2. 用户需求

用户原始要求：默认不使用浏览器验收，显示说明使用浏览器验收时才使用。

## 3. 当前源码事实

- `AGENTS.md` 第 146、181、183 行将浏览器验收或视觉验证写为常规要求。
- 该任务只调整协作文档，不改变产品、源码、依赖、配置或持久化数据。

## 4. 目标与非目标

### 4.1 目标

- 明确浏览器验收默认跳过。
- 保留用户明确要求时的浏览器验收工具优先级与记录要求。

### 4.2 非目标

- 不修改应用源码、测试、依赖或产品验收标准。
- 不删除浏览器验收能力。

## 5. 方案说明

将 `AGENTS.md` 中相关条款改为条件式表述：任务明确要求浏览器验收时，按现有工具优先级执行；否则不启动浏览器验收，也不将其记录为已执行。

## 6. 预计改动与影响评估

- 修改 `AGENTS.md` 的 HTML 文档视觉验证、文档一致性检查和工程验证条款。
- 新增本变更记录，记录实际规则和验证结果。

### 6.1 核心数据结构变化

不涉及数据结构、接口或持久化模型。

### 6.2 上下游与跨模块影响

仅影响后续 Codex 任务的验收流程；自动测试、构建和静态检查规则不变。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 任务遗漏浏览器验收 | 任务本身需要交互验证但未明确说明 | 可能减少覆盖 | 用户或任务说明明确要求时仍执行；必要时回退为原规则 |

## 8. 验证与验收

- 自动测试：不适用，未修改源码。
- 构建与静态检查：不适用，未修改源码。
- 浏览器验收：按本次新规则不执行。
- 持久化与恢复：不适用。
- 成功标准：`AGENTS.md` 明确“默认跳过，仅明确要求时执行”。

## 9. 待确认项与决策

无。用户要求已明确。

## 10. 最终批准方案

用户于 2026-09-03 18:45 CST 直接授权执行：将浏览器验收改为仅在任务明确说明时使用。

## 11. 实施记录

已修改 `AGENTS.md` 中三处浏览器验收规则，未触碰用户已有改动或业务源码。

## 12. 验证结果

- 使用 `rg` 检查 `AGENTS.md` 中相关条款，确认默认规则和条件式例外均已写明；使用 `git diff --check` 确认无空白错误。
- 未运行浏览器验收，符合本次变更要求。

## 13. 文档同步检查

- 产品文档：不涉及。
- 架构文档：不涉及。
- 决策文档：不涉及。
- 文档入口：不涉及。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-09-03 18:45 CST | Codex | 创建并实施规则调整。 |
| 2026-09-03 22:00 CST | Codex | 本次提交请求授权验收，规则与记录已闭环。 |
