# DD-20260901-002：删除上次提交遗留的过程性图片

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已取消 |
| 类型 | 清理 |
| 创建时间 | 2026-09-01 15:23 CST |
| 最后更新 | 2026-09-01 15:26 CST |
| 当前阶段 | 用户决定保留现有图片，未执行删除 |
| 源码基线 | `44d5b43` `feat(scene): 房间初步建模` |
| 实现提交 | 尚未创建 |
| 关联任务 | 删除上一次提交中遗留的过程性图片 |

> 本文是待审阅方案。批准前只允许只读检查和修改本文档。

## 1. 给阅读者的结论

最近一次提交将房间建模过程中的参考图、局部证据图、材质贴图和复核渲染图一并提交，仓库因此保留了大量不参与运行的中间产物。建议删除该提交新增的 47 张图片，并同步清理过程性 JSON/Markdown 中指向这些图片的失效路径；保留源码运行不需要的任何图片不会带来产品能力损失。

当前工作区已有 `docs/assets/concepts/simple-study-room-shell.png` 的未提交删除，以及其他与本任务无关的源码、依赖和架构文档修改。本任务不会覆盖或回退这些已有改动。

## 2. 用户需求

### 用户原始要求

找出并删除上一次提交中留下的过程性图片中间产物。

### Codex 解释

以 `HEAD`（`44d5b43`）相对其父提交新增的图片为候选范围，逐项检查引用后删除过程性图片；不删除运行时实际依赖的图片资源，不处理其他提交中的历史审阅资产。

## 3. 当前源码事实

- `git diff-tree` 显示 `HEAD` 新增 47 张 `.png` 图片，位于 `docs/assets/concepts/` 和 `docs/assets/model-reviews/dd-20260830-002`、`dd-20260831-006`。
- 这些图片未被 `src/` 或 `public/` 的运行时代码直接引用；运行时默认预览使用 `/assets/scene-color-presets/default-v11.webp`，不属于本次提交新增图片。
- 过程性图片被 `docs/assets/model-reviews/dd-20260830-002` 和 `dd-20260831-006` 的分析 JSON/Markdown 作为参考、证据或复核渲染路径引用。
- 当前工作区已有未提交改动：`docs/assets/concepts/simple-study-room-shell.png` 已删除，另有架构、源码、测试和 `package-lock.json` 修改；这些不是本任务实施结果。
- 上述结论由 Git 文件清单、`rg` 引用搜索和当前工作区状态支持。

## 4. 目标与非目标

### 4.1 目标

- 删除 `HEAD` 新增的过程性图片中间产物。
- 清理过程记录中指向已删除图片的失效引用，保持文档引用检查通过。
- 保留运行时资源和用户已有工作区改动。

### 4.2 非目标

- 不修改业务源码、依赖、运行时配置或持久化数据。
- 不删除其他提交中的历史图片资产。
- 不删除仍被产品文档作为正式概念资产使用的图片，除非确认它属于本次用户指定的过程产物范围。
- 不创建提交、推送远端或创建 PR。

## 5. 方案说明

建议以最近提交新增图片清单为边界，保留正式概念资产，删除参考包、候选图、网格细节、材质/PBR 证据和建模复核渲染图。随后仅修正过程性分析文件中的失效图片路径或删除不再有内容的过程性图片引用段落，不改写产品结论和源码事实。

## 6. 预计改动与影响评估

- 删除 `docs/assets/model-reviews/dd-20260830-002/img2threejs/` 下的网格细节图、材质证据图和 PBR 贴图。
- 删除 `docs/assets/model-reviews/dd-20260830-002/room-concept-v1.png`、`room-concept-v2-empty-shell.png`。
- 删除 `docs/assets/model-reviews/dd-20260831-006/img2threejs/` 下的复核渲染图。
- 对应清理 `room-assessment.json`、`room-detail-inventory.json`、`material-analysis.json`、`reference-admission.json`、`image-analysis.md`、`visual-review.md` 等过程记录中的失效图片引用。
- 不主动覆盖当前已有的 `simple-study-room-shell.png` 删除；参考包图片和该正式概念资产的最终处理范围需要批准时明确。

### 6.1 核心数据结构变化

不涉及运行时类型、接口、持久化模型或数据结构；只清理文档过程资产及其路径记录。

### 6.2 上下游与跨模块影响

源码和构建流程不受影响。影响范围限于文档资产、过程复核记录和文档引用检查。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 误删正式参考资产 | 把仍用于产品说明的概念图纳入过程图 | 文档缺少视觉参考 | 执行前按引用和资产命名复核；Git 删除可由本次变更回退 |
| 过程记录出现失效路径 | 只删图片而不清理 JSON/Markdown | 文档引用检查失败 | 同步清理或明确标记已移除的过程引用 |

## 8. 验证与验收

- 自动测试：不涉及业务逻辑；运行最小的路径/引用检查。
- 构建与静态检查：执行项目已有构建命令，确认无源码影响。
- 浏览器验收：不适用，运行时界面不变。
- 持久化与恢复：不适用。
- 成功标准：批准范围内的过程图片已删除；无未解释的失效引用；现有业务测试和构建通过。

## 9. 待确认项与决策

请确认是否按第 6 节执行，并明确以下边界：

- 是否删除最近提交新增的参考包 7 张图片；建议删除，因为它们只服务于本次建模分析。
- 是否保留正式概念资产 `simple-study-room-shell.png`；当前工作区已存在用户删除，建议不回退该删除。

## 10. 最终批准方案

用户于 2026-09-01 15:26 CST 决定保留现有图片，取消本次清理。未执行第 6 节的删除和引用清理。

## 11. 实施记录

本次任务未实施删除。最近提交中的 47 张图片保持不变；用户已有的工作区修改未触碰。

## 12. 验证结果

未运行构建或测试；本次未改变源码和运行时行为。

## 13. 文档同步检查

- 产品文档：无产品行为变化，不修改。
- 架构文档：无当前架构变化，不修改。
- 决策文档：无长期约束变化，不修改。
- 文档入口：不新增入口；需检查过程记录路径。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-09-01 15:23 CST | Codex | 创建待确认方案，完成最近提交图片清单和引用检查。 |
| 2026-09-01 15:26 CST | 用户 | 决定保留现有图片，取消清理。 |
