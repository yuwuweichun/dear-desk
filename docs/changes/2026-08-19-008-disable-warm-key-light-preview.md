# DD-20260819-008：关闭暖色主方向光进行内页颜色对比

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已取消 |
| 类型 | 调研 / 3D 视觉诊断 |
| 创建时间 | 2026-08-19 18:10 CST |
| 最后更新 | 2026-08-21 23:29 CST |
| 当前阶段 | 用户已取消，未改变生产光照 |
| 源码基线 | 当前工作区 HEAD；保留既有未提交诊断记录 |
| 实现提交 | 本次仅提交诊断记录（见 Git 历史）；实验源码未提交 |
| 关联任务 | 临时关闭 `DeskScene` 中暖色主方向光，观察本子左右页颜色差异 |

> 用户明确要求“先关闭我看看效果”，构成直接执行授权。本次仅修改光照强度用于对比，不改变纸张材质或几何。

## 1. 给阅读者的结论

本记录中的临时实验曾计划把暖色主方向光设为 `intensity={0}`，但提交前复核确认当前 `src/scene/DeskScene.tsx` 仍为 `intensity={1.68}`，且 Git 中没有业务源码差异。由于浏览器对比也未完成，本次只能提交诊断记录，不能把“关闭主光后的视觉效果”记为已交付或已验收。

## 2. 用户需求

用户要求先关闭前文提到的暖色主方向光，查看本子完全铺开后的颜色效果。

## 3. 当前源码事实

- 光源位于 `src/scene/DeskScene.tsx` 的 `DeskContents`，当前颜色为 `#fff4d6`、强度为 `1.68`、位置为 `[-5.5, 10.5, 7]`，并承担投影阴影。
- 其余光照不在本次实验中修改：半球光强度 `0.86`、冷色方向光强度 `0.28`、环境光强度 `0.28`。

## 4. 目标与非目标

### 4.1 目标

- 临时关闭暖色主方向光，提供可视对照。
- 保持其他场景条件不变，便于判断色差来源。

### 4.2 非目标

- 不修改纸张 albedo、roughness、法线、页芯几何或开合动画。
- 不把该临时参数视为最终视觉方案。

## 5. 方案说明

将方向光强度设为 0 而不是删除节点，保留原位置、阴影配置和可恢复结构，避免引入额外变量；后续可根据截图决定恢复或调整。

## 6. 预计改动与影响评估

- 修改 `src/scene/DeskScene.tsx` 中暖色方向光的 `intensity` 为 `0`。
- 更新本记录中的实际实施与验证结果。

### 6.1 核心数据结构变化

无。

### 6.2 上下游与跨模块影响

仅影响 Three.js 场景的直接光照输出；不影响持久化、DOM 日记、贴纸或交互状态。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 场景整体偏暗 | 主光贡献较大 | 用户无法判断纸面色差 | 保留其他光源；恢复 `1.68` 即可回退 |
| 阴影对比变化 | 主光同时承担投影 | 桌面与本子阴影变弱 | 本次实验接受该变化，仅用于颜色对比 |

## 8. 验证与验收

- 自动测试：执行定向模型测试、lint 和 build。
- 浏览器验收：使用 `ego-browser` 打开本地 `/studio`，检查桌面端完全铺开状态并截图。
- 成功标准：主暖光强度为 0，页面可正常打开本子，截图可用于与原效果对比。

## 9. 待确认项与决策

待用户查看关闭主光后的视觉效果，再决定恢复、调低强度或继续做法线补偿实验。

## 10. 最终批准方案

用户于 2026-08-19 明确要求先关闭暖色主方向光查看效果，批准本次临时实验清单：将 `intensity` 设为 0，保留其他光源与材质不变，并进行本地页面视觉验证。

## 11. 实施记录

实施过程中曾按记录把 `src/scene/DeskScene.tsx` 中暖色主方向光的 `intensity` 从 `1.68` 临时设为 `0`，但该临时源码改动未保留。提交前复核时，当前工作区和 `HEAD` 均为 `intensity={1.68}`，Git 只包含本记录与关联诊断记录两个未跟踪 Markdown 文件。因此本次提交不改变运行时光照、材质或几何。

## 12. 验证结果

 - `npm test -- --run src/scene/models/model-factories.test.ts`：12 项通过。
 - `npm run lint`：通过。
 - `npm run build`：通过；保留既有大 chunk 警告。
 - `node scripts/check-doc-references.mjs`：通过。
 - `git diff --check`：通过。
 - `ego-browser`：未完成页面截图。开发服务器可以启动，但当前沙箱在命令结束后回收 localhost 后台进程，浏览器收到 `ERR_CONNECTION_REFUSED`；未将其记为视觉通过。
 - 提交前源码复核：`src/scene/DeskScene.tsx` 与 `HEAD` 均为暖色主方向光 `intensity={1.68}`；实验源码没有进入本次提交。

## 13. 文档同步检查

- 产品文档：不变，本次为临时诊断。
- 架构文档：不变，光照所有权不变。
- 决策文档：不变。
- 文档入口：无需新增入口。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-08-19 18:10 CST | Codex | 根据用户直接执行要求创建临时主光关闭实验记录。 |
| 2026-08-19 18:22 CST | Codex | 已将主暖光强度设为 0；自动检查通过，浏览器截图受本机 localhost 进程隔离限制未完成。 |
| 2026-08-19 18:37 CST | 用户 / Codex | 用户要求仅提交当前两个文档。提交前发现源码已恢复为 `1.68` 且没有业务差异，因此回写真实状态为实施中；本次只提交诊断记录，不宣称临时视觉实验已交付。 |
| 2026-08-21 23:29 CST | 用户 / Codex | 用户明确要求取消暖色主光对比实验；实验源码未保留，`DeskScene` 当前 `intensity={1.68}` 不变，状态更新为已取消。 |
