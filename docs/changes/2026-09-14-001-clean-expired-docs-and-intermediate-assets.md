# DD-20260914-001：清理过期变更记录与中间文档资产

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已完成 |
| 类型 | 文档 / 清理 |
| 创建时间 | 2026-09-14 00:00 CST |
| 最后更新 | 2026-09-14 00:25 CST |
| 当前阶段 | 用户已验收并授权创建提交 |
| 源码基线 | 当前工作区 `be3ffa7` |
| 实现提交 | 本次提交 |
| 关联任务 | 清理无用中间文件，并将逾期未验收等记录标记为已过期 |

## 1. 给阅读者的结论

本次只清理文档资产和历史状态，不修改业务源码、运行时资源或持久化数据。阶段性渲染、重复截图、已否决配色截图和铭牌颜色对比截图将删除；正式概念图、最终结论和文档阅读器保留。

所有当前不是“已完成”的变更记录统一改为“已过期”。已过期记录仅保留历史索引，不再作为当前事实、执行方案或验收依据。

## 2. 用户需求

- 删除无用的中间过程文件、截图和重复资产。
- 将未验收等过期状态统一标注为“已过期”。
- 已过期文件内容今后忽略，不再参考。

## 3. 当前源码事实

- `docs/assets/model-reviews/` 主要保存阶段性建模审查资产，当前约 135 MB。
- `docs/palette-review/assets/v01..v10/` 保存已否决配色截图。
- `docs/assets/nameplate-color-comparison/` 保存已不作为最终选择的铭牌颜色对比截图。
- 运行时资源位于 `public/`，本次不删除。

## 4. 目标与非目标

### 4.1 目标

- 删除已确认无当前用途的中间资产和重复资产。
- 把所有状态不是“已完成”的变更记录标记为“已过期”。
- 在文档入口和本记录中说明“已过期”记录不再作为当前事实。

### 4.2 非目标

- 不修改业务源码、依赖、工程配置或运行时资源。
- 不删除正式概念参考图、最终结论文档、架构文档或产品文档。

## 5. 方案说明

采用最小清理范围：删除阶段性渲染和重复图像，保留能解释最终决策的少量文档与正式参考资产；对历史变更记录只改状态和失效说明，不重写历史正文。

## 6. 预计改动与影响评估

- 删除 `docs/assets/model-reviews/dd-20260810-002/`、`dd-20260816-002/`、`dd-20260817-001/`、`dd-20260817-002/` 的中间审查资产。
- 删除已否决配色截图、铭牌颜色对比截图和房间概念图旧版本。
- 更新所有非“已完成”变更记录的状态字段与过期提示。
- 更新文档入口，避免继续把过期记录作为当前状态展示。

### 6.1 核心数据结构变化

不变。无源码、数据库或运行时数据变化。

### 6.2 上下游与跨模块影响

仅影响文档资产和历史变更记录的可读性；运行时构建不读取 `docs/` 资产。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 历史截图仍有复核价值 | 后续需要重看阶段过程 | 文档复核细节减少 | 保留最终结论、正式参考图和任务记录；删除前确认精确目录 |
| 过期记录被误当成当前方案 | 入口继续展示旧状态 | 后续执行依据混乱 | 统一状态为“已过期”，并在入口增加解释 |

## 8. 验证与验收

- 自动检查：删除后检查目标路径不存在、保留路径存在。
- 文档检查：运行文档引用检查，确认无失效引用。
- Git 检查：确认仅有文档与文档资产变化。
- 浏览器验收：不需要。

## 9. 待确认项与决策

用户已明确授权直接清理并使逾期记录失效，无待确认项。

## 10. 最终批准方案

2026-09-14 用户明确要求直接执行。执行清单见第 6 节。

## 11. 实施记录

已完成：

- 删除四个旧模型审查任务中的阶段渲染、comparison pass、detail zone、blockout 代码和部分中间材质证据目录。
- 删除房间 img2threejs 的细节 inventory 与材质证据图片。
- 将 31 份原为待验收、实施中、已取消或已替代的变更记录状态统一改为“已过期”。
- 保留正式概念图、最终渲染、最终报告、运行时资源和配色审查入口。

## 12. 验证结果

已验证：

- `node scripts/check-doc-references.mjs` 通过。
- 变更记录状态统计为：96 份“已完成”、31 份“已过期”，本记录 1 份“待验收”。
- `docs/assets/` 从约 160 MB 降至约 105 MB。
- 未修改 `src/`、`public/`、依赖或持久化数据。

## 13. 文档同步检查

- 产品文档：不变；过期规则记录在本任务中，不改变产品范围。
- 架构文档：不变。
- 决策文档：不变。
- 文档入口：实施后检查。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-09-14 00:00 CST | Codex | 根据用户要求创建清理任务并直接执行。 |
| 2026-09-14 00:18 CST | Codex | 完成中间资产清理、31 份历史记录过期标记和文档引用检查；状态改为待验收。 |
| 2026-09-14 00:25 CST | 用户 | 验收通过并明确要求创建提交。 |
