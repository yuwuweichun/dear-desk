# DD-20260901-010：按当前代码修正 V12 调色板断言

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 状态 | 已完成 |
| 类型 | 修复 |
| 创建时间 | 2026-09-01 17:40 CST |
| 最后更新 | 2026-09-01 17:42 CST |
| 当前阶段 | 已验证并提交 |
| 源码基线 | 3f53b75 |
| 实现提交 | 本次提交 |
| 关联任务 | 以当前代码为准修复 3 个 V12 颜色测试断言 |

## 1. 给阅读者的结论

测试仍断言旧版 V12 颜色，导致完整测试出现 3 个失败。当前源码已将 V12 默认值设为背景 `#8FA89E`、工作面 `#2F432D` 和垫体/包边 `#283E29`；本次只同步测试期望值，不改变生产代码。

## 2. 用户需求

用户要求以当前代码为准修复测试断言问题。

## 3. 当前源码事实

- `src/scene/models/material-library.ts` 将 `DEFAULT_SCENE_PALETTE_VERSION` 设为 `v12`，V12 使用 `#8FA89E`、`#2F432D`、`#283E29`。
- `src/scene/models/model-factories.test.ts` 仍对默认 V12 断言旧值 `#d5dad8`、`#423f2c`、`#303021`，另有一个断言比较大小写不一致的十六进制值。
- 上次 `npm run check` 中仅该测试文件的 3 个测试失败，其他 28 个测试文件通过。

## 4. 目标与非目标

### 4.1 目标

- 更新过时的 V12 测试断言，使其匹配当前源码。
- 保持历史 V1-V11 断言不变。

### 4.2 非目标

- 不修改生产代码、颜色定义或产品行为。
- 不重写测试结构，不处理与本次失败无关的测试。

## 5. 方案说明

仅修改 `model-factories.test.ts` 中默认 V12 的旧色值和大小写期望值，复用源码中的当前 V12 定义作为事实来源。

## 6. 预计改动与影响评估

- 修改 `src/scene/models/model-factories.test.ts` 的 3 处失败断言对应期望值。
- 新增本变更记录。

### 6.1 核心数据结构变化

无。仅测试断言变化。

### 6.2 上下游与跨模块影响

无生产运行时影响；仅恢复模型工厂测试与当前 V12 颜色定义的一致性。

## 7. 风险、边界与回退

| 风险 | 发生条件 | 影响 | 缓解与回退 |
| --- | --- | --- | --- |
| 测试误把历史候选改成 V12 | 修改范围扩大到 V1-V11 | 丢失历史回归保护 | 仅改默认 V12 断言，保留历史值 |

## 8. 验证与验收

- 自动测试：运行 `npm run test -- src/scene/models/model-factories.test.ts` 和 `npm run test`。
- 构建与静态检查：运行 `npm run check`。
- 浏览器验收：不适用，生产代码不变。
- 持久化与恢复：不适用。
- 成功标准：模型工厂测试通过，完整检查不再因旧 V12 断言失败。

## 9. 待确认项与决策

无。用户已明确要求以当前代码为准。

## 10. 最终批准方案

用户于 2026-09-01 17:40 CST 明确要求直接修复断言。执行清单：更新 `model-factories.test.ts` 的 V12 期望值，运行测试与检查，记录结果并提交。

## 11. 实施记录

已实施：

- `src/scene/models/model-factories.test.ts` 将默认 V12 断言更新为当前源码的 `#8FA89E`、`#2F432D`、`#283E29`。
- 颜色格式正则改为大小写不敏感；Three.js 颜色值比较统一为小写，保留历史候选断言。

## 12. 验证结果

- `npm run check`：通过。
- 测试：29 个测试文件通过，125/125 通过。
- lint：通过。
- build：通过；仅有既有的大 chunk warning。
- 文档引用检查：通过。
- 浏览器验收：不适用，生产代码未修改。

## 13. 文档同步检查

- 产品文档：无需修改，产品文档已记录当前 V12 颜色。
- 架构文档：无需修改，架构文档已记录当前 V12 颜色。
- 决策文档：无需修改。
- 文档入口：无需修改。

## 14. 审阅记录

| 时间 | 参与者 | 记录 |
| --- | --- | --- |
| 2026-09-01 17:40 CST | Codex | 创建并批准断言修复记录。 |
| 2026-09-01 17:41 CST | Codex | 完成测试断言修复并通过完整检查，状态更新为待验收。 |
| 2026-09-01 17:42 CST | Codex | 用户明确要求提交；源码、测试、文档和验证均完成，状态更新为已完成。 |
